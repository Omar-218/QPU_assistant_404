# سجل العضو 2 — صفحات الطالب (الواجهة العامة)

> ملف واحد فقط طوال المشروع. أضف إدخال جديد أعلى الملف (الأحدث بالأعلى) بعد كل جلسة عمل.
> لا تحذف الإدخالات القديمة.
> ⚠️ الملفات المملوكة لك محددة بخطة البناء (team-plan-v2.md) القسم 3. لا تعدّل أي ملف خارجها.

---

## [التاريخ: 2026-07-21 — إعادة التوزيع: عقد الأنواع (pdf/image/link/note)]

### ما تم إنجازه
- **`LectureItem.jsx`:** فرّعت العرض حسب `item.type`:
  - `pdf`/`image` (وأي عنصر بدون `type` بالبيانات القديمة) → نفس زر التوگل السابق حرفياً، يبلّغ الأب عبر `onToggle`/`isOpen`.
  - `link` → عنصر `<a target="_blank" rel="noreferrer">` مباشر بدون توگل ولا أي حالة، مبني على `item.url`.
  - `note` → توگل محلي (state داخل المكوّن نفسه فقط) يعرض `item.content` inline أسفل الزر مباشرة بـ `white-space: pre-wrap`، بدون عارض منفصل ولا طبقة ملء شاشة.
- **`FileViewer.jsx`:** أضفت prop اختياري `type` (افتراضي `"pdf"` — توافق عكسي). لو `"image"` يعرض `<img className="object-contain">` بدل `<iframe>`؛ باقي الشريط (تنزيل/فتح بتبويب جديد/إغلاق/Escape) بلا أي تغيير.
- **`Subject.jsx`:**
  - أضفت `itemKey(item, idx)` كمفتاح React/state موحّد، لأن `item.file` صار غير مضمون الوجود بعد اليوم (`link` يحمل `url` بدل `file`، و`note` يحمل `content` فقط بلا `file`/`url`). الأولوية: `file` → `url` → فولباك `type-title-index`.
  - حالة `openFile` بالأب (ومعها `FileViewer`) صارت تُفعَّل فقط لما `isViewerType` (يعني `pdf`/`image`) — عناصر `link`/`note` لا تلمس حالة الأب إطلاقاً، كل واحد مستقل بذاته داخل `LectureItem`.
  - مرّرت `type` لـ `FileViewer` عند فتحه.

### مشكلات واجهتها
- ما فيه `id` صريح بشكل العنصر بالعقد المُتفق عليه (فقط `type`/`title`/`section`/`hidden`/`file`/`url`/`content`)، فاضطريت أبني مفتاح فولباك (`itemKey`) بدل الاعتماد على `item.file` كما كان بالكود القديم. لو انضاف `id` لاحقاً بالعقد، هالفولباك ينشال بسهولة.

### قرارات اتخذتها (ولماذا)
- خليت حالة `note` توگل محلي داخل `LectureItem` نفسه (لا حالة بالأب) لأنه ما يحتاج تنسيق مع أي عنصر ثاني مفتوح (بعكس pdf/image اللي يشتركون بعارض ملء شاشة واحد بالأب) — أبسط وأقل تزامن.
- خليت `link` بلا أي حالة إطلاقاً (لا توگل ولا isOpen) لأنه فتح مباشر بتبويب جديد، ما فيه شيء "يُفتح/يُغلق" داخل الصفحة.
- أبقيت شكل زر pdf/image واستايله كما كان تماماً بدون أي تغيير، حرصاً على عدم كسر أي سلوك موجود بالإنتاج.

### هل احتجت تعديل ملف خارج نطاق ملكيتك؟ (يُفترض دائماً "لا" بهذا التصميم)
- لا — الثلاثة ملفات المعدّلة (`Subject.jsx`، `LectureItem.jsx`، `FileViewer.jsx`) ضمن نطاقي بالتوزيع الجديد، ولم ألمس أي ملف من عضو 3 أو عضو 5.

### ملاحظات للمدير
- بانتظار تسليم عضو 3 (`SubjectForm.jsx`/`FileUploaderWidget.jsx`) وعضو 5 (`githubPublisher.js`) عشان أتأكد فعلياً إن شكل `items` الواصل من `lectures.json` مطابق تماماً للعقد المفترض (خصوصاً حقل `url` وتحقق http/https المفروض يكون بالطرفين).
- ما زلت أعتمد فولباك المفتاح المذكور أعلاه (`itemKey`) — لو تحب نثبّت `id` صريح بالعقد بدل الفولباك، أقدر أعدّله بسرعة بعد ما يتفق الجميع.

---

## [التاريخ: 2026-07-19 — الدفعة 4]

### ما تم إنجازه
- **المهمة 3:** حوّلت `FileViewer.jsx` لطبقة `position: fixed; inset: 0` تغطي الشاشة كاملة (تخفي Sidebar/Header)، مع زر إغلاق واضح + إغلاق بمفتاح Escape، وأضفت زر "تنزيل" (`download`) بجانب "فتح بتبويب جديد" الموجود أصلاً. لا Route جديد — نفس حالة `openFile` بـ `Subject.jsx`.
- **المهمة 4:** أنشأت `hooks/useFavorites.js` و`hooks/useRecentlyViewed.js` (localStorage، حسب العقد المطلوب بالضبط)، و`components/layout/FavoritesSection.jsx` و`components/layout/RecentlyViewedSection.jsx` (تستهلكان `useStudyPlan()` من عضو 4 + الـ hooks الخاصة بي، وتُخفيان نفسيهما بالكامل لو القائمة فاضية).
- أضفت زر "⭐ تفضيل" بـ `SubjectCard.jsx` (قراري: هناك وليس بـ `Subject.jsx`، لأن التصفح بالبطاقات هو السياق الطبيعي للتفضيل).
- أضفت استدعاء `addVisit(id)` بـ `Subject.jsx` عند نجاح تحميل المادة.
- **بالمناسبة (إصلاح غير مطلوب صراحة لكن بنفس ملفاتي):** رجّعت `SubjectList.jsx`/`StudyPlan.jsx` ليستهلكا `useStudyPlan()` بدل fetch مباشر لـ `study-plan.json` — يحل الازدواجية المذكورة بخطة الدفعة نفسها. وصححت مسار PDF بـ `Subject.jsx` من `/pdf/...` الثابت إلى `${import.meta.env.BASE_URL}pdf/...` (نفس درس مسارات fetch المطلقة).

### مشكلات واجهتها
- لا شيء يمنع البناء؛ اعتمدت على عقد `useStudyPlan()` الموثّق بخطة الدفعة (`{ courses, loading }`) رغم أن الملف نفسه من مسؤولية عضو 4 ولم يصل بعد وقت كتابة هذا.

### قرارات اتخذتها (ولماذا)
- زر التفضيل بالبطاقة فقط (`SubjectCard.jsx`) لا بصفحة المادة، لتفادي ازدواجية مكان القرار وتبسيط الواجهة.
- إبقاء عارض الملف كمكوّن واحد (`FileViewer.jsx`) بدل تفكيكه لصفحة/Route منفصلة، حسب توجيه خطة الدفعة.

### هل احتجت تعديل ملف خارج نطاق ملكيتك؟ (يُفترض دائماً "لا" بهذا التصميم)
- لا — الملفات الجديدة بـ `components/layout/` (وليس `Sidebar.jsx`/`Header.jsx` نفسيهما) ضمن نطاقي حسب توزيع خطة الدفعة، ولم ألمس أي ملف مملوك للمدير.

### ملاحظات للمدير
- **خارج نطاقي لكن يستحق الانتباه:** ملاحظة هندسية على المهمتين 1 و2 (ليستا من عملي): تخزين GitHub PAT بصلاحية `contents:write` بالمتصفح (حتى بـ sessionStorage) على موقع عام منشور، ثم استخدامه لدمج Pull Requests تلقائياً بلا مراجعة بشرية افتراضياً — يعني أي كود بالمتصفح (امتداد متصفح ضار، XSS مستقبلي، إلخ) يقدر يدمج تغييرات بالريبو مباشرة بلا أي بوابة مراجعة. يستاهل تفكير إضافي قبل التفعيل الافتراضي، حتى لو الخيار المتقدم لإيقافه موجود.
- تأكدت أن `useSubjectData.js` (عضو 4) لسه يستخدم مسار fetch مطلق `/data/subjects/...` بدل `BASE_URL` — نفس الثغرة اللي صححتها بملفي، لكنه ملف مو من صلاحيتي.

---

## [التاريخ: 2026-07-25 — ترتيب صفحة "المواد الدراسية" بالسنة/الفصل]

### المشكلة
`SubjectList.jsx` كان يعرض المواد بترتيب `study-plan.json` الخام — وهو ترتيب إدراج (بحسب وقت إضافة
كل مادة فعلياً)، بلا أي معنى أكاديمي. طلب المستخدم ترتيبها بالسنة والفصل بدلاً من هذا.

### الحل
لم أكتب منطق تسطيح `curriculum.json` من الصفر — استخدمت `fetchFlatCurriculum()` الجاهزة أصلاً بـ
`lib/curriculum.js` (نفس المصدر اللي يغذّي `CurriculumCoursePicker` بلوحة التحكم)، اللي ترجع أصلاً
`{ id, year, level, ... }` لكل مادة بالخطة الرسمية كاملة (بما فيها `electiveGroups` بعد إصلاح سابق
موثَّق بنفس الملف). بنيت منها `Map<id, {year, level}>` عند تحميل الصفحة، ورتّبت `courses` (من
`useStudyPlan`) حسب `(year, level)` تصاعدياً قبل تمريرها لـ Fuse.js/العرض.

- **مواد بلا سنة محدَّدة** (متطلبات الجامعة الاختيارية بـ `electiveGroups`، مثل الصحة العامة
  والعربي 2) تُدفَع لنهاية القائمة دائماً (`Infinity` كقيمة افتراضية) — لا سنة فعلية لها بالخطة
  الرسمية أصلاً، هذا سلوك صحيح لا خلل.
- **الترتيب الجديد يُطبَّق فقط على العرض الافتراضي (بلا بحث)** — نتائج البحث النشط تبقى مرتَّبة حسب
  درجة تطابق Fuse.js كما كانت، لأنها أفيد بسياق البحث من ترتيب المنهج.
- ريثما تتحمّل بيانات `curriculum.json` (فرق ميلي ثانية عادةً)، تُعرض القائمة بترتيبها الخام مؤقتاً
  بدل شاشة تحميل إضافية — لا وميض ملحوظ عملياً.

### اختبارات نفّذتها
- `npm run build`: نجح كاملاً.
- سكربت Node منفصل يحاكي نفس منطق الترتيب مباشرة ضد `curriculum.json`/`study-plan.json` الفعليين:
  تحقّقت يدوياً أن كل مادة بموضعها الصحيح (سنة 1 مستوى 1 → سنة 1 مستوى 2 → سنة 2 مستوى 3 → سنة 2
  مستوى 4 → المواد الاختيارية بالنهاية)، بما فيها "رياضيات (2)" المضافة حديثاً (سنة 1 مستوى 2،
  بالموضع الصحيح بين "برمجة (1)" و"الدارات الكهربائية").

### هل احتجت تعديل ملف خارج نطاق ملكيتك؟
- لا. `pages/SubjectList.jsx` فقط (استيراد إضافي من `lib/curriculum.js`، بلا تعديل عليه).

## [التاريخ: 2026-07-26 — تطبيق إصلاح القائمة المنسدلة (كان مفقوداً بهذا الفرع)]

### ما حصل
إصلاح "قائمة منسدلة بدل عارض ملء الشاشة" (موثَّق أعلاه بتاريخ 2026-07-24) اتضح إنه لم يُطبَّق فعلياً
على هذا الفرع من المستودع — سُلِّم كحزمة منفصلة يومها لكن ما انطبقت على GitHub قبل رفع نسخة أحدث.
بالتزامن، هذا الملف تطوَّر بشكل مستقل بميزات جديدة (أحداث نشِطة، أزرار طلب حدث/رفع ملف، أيام
الدوام الأسبوعي `scheduleDays`) — فطبّقت الإصلاح الآن بدمج مستهدَف (مو استبدال كامل للملف) عشان ما
تنفقد أي من هذي الإضافات:

- حذفت استيراد/استخدام `FileViewer` فقط، وأبقيت كل شيء آخر بالملف كما هو تماماً (شريط الأحداث،
  زرّا اقترح حدثاً/اطلب رفع ملف، عرض `scheduleDays`).
- بسّطت `openFile` (كائن كامل) لـ `openKey` (مفتاح نصي فقط) بنفس منطق الإصلاح الأصلي — بلا أي أثر
  على `openForm` (حالة منفصلة تماماً لنماذج الأحداث/الرفع، لم تُمس).
- `LectureItem.jsx`: نسخته الحالية بهذا الفرع كانت مطابقة 100% لنسخة ما قبل الإصلاح (بحثت بـ diff
  صريح للتأكد) — فنسختها مباشرة من الحزمة الأصلية بلا أي دمج يدوي، صفر مخاطرة فقدان بيانات.
- حذفت `FileViewer.jsx` نهائياً (تحقّقت أنه غير مستخدَم بأي مكان آخر بالمشروع).

### اختبارات نفّذتها
- `esbuild` على الملفين: بلا أخطاء. `npm run build` كامل: نجح (حزمة `Subject.js` أصغر بعد حذف
  الاستيراد الميت، كما متوقَّع).
- `grep` شامل: صفر `import ... FileViewer` متبقٍّ بكامل `src/`.

### هل احتجت تعديل ملف خارج نطاق ملكيتك؟
- لا. `pages/Subject.jsx`, `components/subject/LectureItem.jsx` (حذف `FileViewer.jsx` معهما).

### ملاحظات للمدير
- **هذا النمط تكرَّر مرتين الآن بنفس الجلسة** (إصلاح الكاش بـ`AdminSubjectEditor.jsx` سابقاً، وهذا):
  إصلاحات تُسلَّم كحزم منفصلة لكن لا تنطبق فعلياً على GitHub قبل رفع نسخة أحدث من المشروع، فترجع
  المشاكل القديمة تلقائياً. يستحق تفكير بعملية واضحة لضمان تطبيق كل حزمة مسلَّمة فعلياً (مثلاً:
  قائمة تحقق قصيرة قبل رفع أي نسخة جديدة للمراجعة).
