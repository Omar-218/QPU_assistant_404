# سجل المدير — assistant404-v2 / QPU_assistant_404

> ملف واحد فقط طوال المشروع. إدخال جديد أعلى الملف (الأحدث بالأعلى) بعد كل جلسة عمل.
> لا تُحذف الإدخالات القديمة.
> الدور: تنسيق المهام، حسم العقود المشتركة قبل التنفيذ المتوازي، حل التعارضات، مراجعة كل تسليم قبل اعتماده.

---

## [التاريخ: 2026-08-03 — جلسة 24: مراجعة مستقلة شاملة لموجة لوحة التحكم + إصلاح خلل حقيقي]

### السياق
بطلب المستخدم ("تأكيد والتحقق من تمام العمليات") ثم تفويض مفتوح ("باشر بما تراه مناسباً"). راجعت
كل الاقتراحات المُنفَّذة (#1-#9، جلسات 21-23) ضد الكود الفعلي مباشرة — لا اعتماداً على أوصاف
`manager-log.md` نفسه. كل التسعة موجودة ومترابطة فعلياً بجودة جيدة (لا ملفات معزولة بلا استخدام).

### خلل حقيقي وجدته ومنفصل تماماً عن أي اقتراح من العشرة
**بعد أي نشر ناجح فعلي (حذف مادة فردي، أو تحديث ظهور جماعي #4) — قائمة المواد بـ`AdminHome.jsx` لم
تكن تُعاد جلبها إطلاقاً.** الأدمن يرى النشر ينجح (commit فعلي على GitHub)، لكن الواجهة تبقى بحالة
قديمة صامتة حتى يُحدِّث الصفحة يدوياً بنفسه. تحديداً:
- مسار الحذف الفردي: `<PublishPanel pkg={pkg} />` بلا `onPublishSuccess` إطلاقاً — بعد حذف ناجح،
  الواجهة تبقى عالقة تعرض حالة "جارِ الحذف" لمادة محذوفة فعلاً بالفعل، للأبد.
- مسار التحديد الجماعي: `onPublishSuccess` كانت موجودة لكنها فقط تمسح `bulkPkg`/`selectedIds`
  المحليّين، بلا أي إعادة جلب لـ`studyPlan` الفعلية من الخادم.

**الإصلاح**: دالة `refreshSubjects()` جديدة (إعادة استدعاء `fetchStudyPlan()` نفسها)، مربوطة الآن
بكلا المسارين. أضفت أيضاً تنظيف `localOverrides` للمعرّفات المنشورة فعلياً بالتحديد الجماعي — وإلا
معاينة محلية سابقة (`toggleLocalHidden`) كانت ستطغى بصمت على حالة الخادم الحقيقية الجديدة بعد
التحديث (تفاعل جانبي لم يُلاحَظ بأي من جلسات 21-23).

### لماذا هذا الخلل تحديداً لم يُكتشَف بالجلسات الثلاث السابقة
كل من #4 (التحديد الجماعي) والحذف الفردي بُنيا/عُدِّلا بجلسات منفصلة (18/21 مقابل قديمة جداً من
جلسة 4)، ولم تُختبَر السلسلة الكاملة "نشر ناجح فعلي ← هل القائمة تعكس الواقع الجديد؟" بأي منهما —
كل جلسة ركّزت على الميزة الجديدة المطلوبة تحديداً، لا على تدفّق ما بعد النجاح الشامل. هذا بالضبط نوع
الخلل الذي يظهر فقط بمراجعة أفقية عبر كل المسارات معاً، لا بمراجعة كل ميزة بمعزل عن غيرها.

### تتمة المراجعة (بطلب "اكمل مراجعات مماثلة"): فحصت كل استخدام لـ`PublishPanel` بالمشروع
النمط اللي اكتشفته أعلاه (نشر ناجح بلا تحديث حالة) تكرَّر بمكانين إضافيين — **نفس البنية بالضبط**،
والمفارقة: كلاهما عُدِّلا بنفس جلسة #6 (تحذير المغادرة) التي طبَّقت الحل الصحيح بـ`SubjectForm.jsx`
لكن لم تُطبّقه هنا رغم تطابق البنية الكامل:

1. **`StudyPlanEditor.jsx`**: `changed` (المُستخدَمة لعرض `PublishPanel` ولـ`useUnsavedChangesWarning`)
   محسوبة بمقارنة `original.courses` مقابل `courses` الحالية. بلا `onPublishSuccess`، `original` تبقى
   القديمة للأبد بعد نشر ناجح → **تحذير مغادرة كاذب** يظهر للأدمن مباشرة بعد نجاح النشر تحديداً (أسوأ
   من خلل AdminHome — هذا نشِط فوراً، لا يحتاج أدمن يحاول يستخدم الصفحة أكثر). أصلحته:
   `onPublishSuccess={() => setOriginal({ courses })}`.
2. **`AdminSectionsManager.jsx`**: نفس النمط — `dirty` من مقارنة `sections` مقابل
   `initialSectionsRef.current`، + `replacements` (استبدالات ملفات معلَّقة) لا تُصفَّر. نفس تحذير
   المغادرة الكاذب، **بالإضافة** لخطر حقيقي: لو الأدمن ضغط "نشر" مرة ثانية بالغلط (يظنّ إنه لم يُحفَظ)،
   نفس استبدالات الملفات تُطبَّق مجدداً. أصلحته: `initialSectionsRef.current = sections` +
   `setReplacements({})` بـ`onPublishSuccess`.

تحققت أيضاً من `AdminRequestsQueue.jsx` (قبول/رفض الطلبات) — **سليمة فعلياً**، `onResolved` مربوطة
بشكل صحيح بـ`handleResolved` بالمكوّن الأب (إزالة فورية من القائمة المحلية). لا خلل هناك.

### تحقّق نهائي
تجميع كامل الشجرة بعد كل الإصلاحات الثلاث (348.2kb): نجح بالكامل بلا أخطاء.

---

## [التاريخ: 2026-08-02 — جلسة 23: تنفيذ الاقتراحين المتبقيين المضبوطي النطاق (#4/#5)]

### السياق
بطلب مباشر من المستخدم ("قم بعمل الباقي") بعد مراجعة توجيه جلسة 22 (كل الاقتراحات العشرة باستثناء
#4/#5/#10/#11). نفّذت #4 و#5 هذي الجلسة — مضبوطا النطاق ولا يحتاجان بنية إضافية حقيقية (خلافاً لـ
#10 جدولة نشر و#11 صلاحيات متدرّجة، ما زالا مؤجَّلين، راجع "توجيه للفريق" أسفل).

### ما تم إنجازه
1. **#4 إجراءات جماعية**: `AdminHome.jsx` — checkbox لكل مادة بالقائمة *المفلترة/المبحوثة حالياً*
   (اقتراح #1) + "تحديد الكل" + زرَّي "إخفاء المحدَّد"/"إظهار المحدَّد". يبني حزمة نشر حقيقية واحدة
   (`buildStudyPlanUpdate` على كامل `subjects` بعد تعديل `hidden` للمعرّفات المحدَّدة فقط) وتُعرَض
   عبر `PublishPanel` مباشرة — بخلاف `toggleLocalHidden` الموجود مسبقاً (معاينة محلية بحتة تحتاج فتح
   كل مادة يدوياً)، هذا نشر فعلي لعدة مواد بنفس commit/PR. **عمداً بلا حذف جماعي** — الخبير ذكر فقط
   مثال الإخفاء/الإظهار، والحذف الجماعي يتعارض مع قرار المدير الأمني الحالي (PR مراجعة دائماً، بلا
   استثناء)؛ توسعة الحذف الجماعي تحتاج قراراً منفصلاً صريحاً لو أراده المستخدم لاحقاً.
2. **#5 سجل تدقيق مرئي**: صفحة جديدة `/admin/audit-log` (`AdminAuditLog.jsx`) — قراءة فقط بالكامل،
   تسرد GitHub Commits API لنفس الريبو (نفس نمط توكن/رؤوس `usePendingRequestsCount.js`)، برسالة كل
   commit + المؤلف + التاريخ + رابط مباشر لتفاصيله الكاملة على GitHub (لا إعادة بناء diff هنا —
   عرض تلخيصي فقط، كما اقترح الخبير حرفياً: "تُبنى فوق GitHub Commits API... بيانات موجودة أصلاً").
   ترقيم صفحات بسيط (الأحدث/أقدم) بلا تحميل كل التاريخ دفعة واحدة.
3. **ربط #9 بـ#5 (كانت نقطة معلَّقة صراحة بجلسة 22)**: `useLastPublishDate.js` (جديد، هوك صغير بنفس
   نمط `usePendingRequestsCount.js`) يجلب تاريخ آخر commit فقط، يُضاف الآن لشريط الإحصائيات بـ
   `AdminHome.jsx` ("🕓 آخر نشر") — يختفي بصمت بلا توكن، لا تخمين.

### ملفات جديدة (بلا تعارض ملكية)
`src/pages/Admin/AdminAuditLog.jsx`, `src/hooks/useLastPublishDate.js` — بالإضافة لتعديلات على
`AdminHome.jsx` (عضو 3) و`App.jsx` (ملف المدير — تسجيل مسار `/admin/audit-log` الجديد).

### تحقّق مستقل
تجميع كامل الشجرة عبر `esbuild --bundle` (نفس أسلوب الجلسات السابقة، بيئتي بلا اتصال شبكة لتشغيل
`npm run build`/`vitest` فعلياً): نجح بالكامل (316.7kb)، كل مسارات الاستيراد الجديدة تحلّ صحيحاً.
**لم أشغّل متصفحاً فعلياً** — الاختبار البصري النهائي (سلوك التحديد الجماعي على الهاتف تحديداً،
شكل سجل التدقيق) يحتاج اختباراً بشرياً فعلياً.

### قرارات اتخذتها (ولماذا)
- **التحديد الجماعي يعمل على القائمة المفلترة/المبحوثة فقط، لا كل subjects**: سلوك متوقَّع — لو
  الأدمن مصفٍّ بحثاً بالتو، "تحديد الكل" يجب ألا يشمل مواد غير ظاهرة أمامه أصلاً.
- **سجل التدقيق بلا أي تصنيف/فلترة برمجية لنوع العملية**: تخمين هش (رسالة الـcommit قد تتغيّر بكود
  عضو 5 بلا عقد شكل صريح) — عرض النص الكامل + رابط GitHub المباشر أوثق وأبسط.
- **#10/#11 يبقيان مؤجَّلين**: كلاهما يحتاج فعلياً بنية إضافية حقيقية (GitHub Action مجدولة لـ#10،
  إعادة تفكير كامل بآلية المصادقة أبعد من توكن خام لـ#11) لا مجرد واجهة فوق بيانات موجودة — نفذتهما
  الآن كان سيعني قراراً معمارياً كبيراً بلا تخطيط مسبق، بالضبط ما حذَّر منه الخبير نفسه.

### توجيه للفريق
لا عمل جديد مطلوب من أي عضو. **#10 (جدولة نشر) و#11 (صلاحيات متدرّجة) يبقيان بانتظار طلب صريح
منفصل من المستخدم مع تخطيط مسبق** — الأول يحتاج تصميم GitHub Action + "طابور نشر مؤجَّل" جديد كلياً،
الثاني يحتاج حسم آلية مصادقة بديلة عن التوكن الموحَّد الحالي قبل أي تنفيذ.

---

## [التاريخ: 2026-08-02 — جلسة 22: الموجة الثانية من مراجعة خبير للوحة التحكم (اقتراحات #7/#8/#9)]

### ما تم إنجازه
1. **#7 فاحص روابط مكسورة**: صفحة جديدة `/admin/link-checker` (`AdminLinkChecker.jsx`) — يفحص كل
   المواد وكل ملفات محاضراتها (**كل professorVariants، لا الدكتور النشِط فقط** — فحص شامل حقيقي)،
   ويرسل طلب `HEAD` لكل رابط pdf/image فعلي منشور (نفس دالة بناء الرابط بالضبط من `Subject.jsx`).
   بلا أي استخدام لـ GitHub API — فحص عبر الموقع المنشور نفسه فقط (ما يراه الطالب فعلياً)، بدفعات 8
   طلبات متزامنة (لا كل الملفات دفعة واحدة، تفادياً لضغط مئات الطلبات على CDN بنفس اللحظة). نتائج
   مجمَّعة حسب المادة، وتمييز صريح بين "مكسور جزماً" (404) و"تعذّر التأكّد" (فشل شبكة أثناء الفحص
   نفسه — لا يُعامَل كمكسور جزماً، تفادياً لنتائج كاذبة لو انقطع نت الأدمن لحظياً).
2. **#8 نسخة احتياطية كاملة**: `src/lib/fullBackup.js` (جديد) — `exportFullBackupZip()` يجمع كل
   `public/data/**` الحالية المنشورة فعلياً (`study-plan.json`, `curriculum.json`, `events.json`,
   `upload-requests-log.json`, وكل `subject.json`/`lectures*.json` — **كل الدكاترة لكل مادة**) بملف
   ZIP واحد. **نطاق متعمَّد** (بالضبط كما اقترح الخبير حرفياً "data/**"): JSON فقط، لا `public/pdf/**`
   الفعلية — غير عملي تحميلها دفعة واحدة عبر المتصفح، وأصلاً لها نسخة احتياطية طبيعية بتاريخ commits
   الريبو (خلافاً لـ data/**، فقدانها أخطر لأنها المصدر الوحيد لبناء كل صفحة). زر "⭳ نسخة احتياطية
   كاملة" جديد بجانب "طلبات الطلاب" بـ `AdminHome.jsx`.
3. **#9 لوحة إحصائيات بسيطة**: شريط صغير أعلى قائمة المواد بـ `AdminHome.jsx` — إجمالي/ظاهر/مخفي، من
   `subjects` المحمَّلة أصلاً بلا أي طلب شبكة إضافي. **لم أضِف "آخر تاريخ نشر"** كما اقترح الخبير —
   لا مصدر بيانات موثوق له بلا استدعاء GitHub Commits API (ميزة #5 "سجل تدقيق مرئي" المنفصلة تماماً،
   لم تُنفَّذ هذي الجلسة)، فأبقيت النطاق للبيانات المتاحة فعلاً بلا تخمين.

### ملفات جديدة (بلا تعارض ملكية)
`src/pages/Admin/AdminLinkChecker.jsx`, `src/lib/fullBackup.js` — بالإضافة لتعديلات على
`AdminHome.jsx` (عضو 3) و`App.jsx` (ملف المدير — تسجيل مسار `/admin/link-checker` الجديد).

### تحقّق مستقل
`npm install` + `vite build` فعلي — بناء ناجح، 96 وحدة، `AdminLinkChecker` بحزمة (chunk) منفصلة
بفضل `lazy()`. لاحظت أن `node_modules` كانت محذوفة من جلسة سابقة (خطوة تنظيف قبل التغليف) فأعدت
تثبيتها أولاً قبل البناء.

### توجيه للفريق
كل الاقتراحات العشرة المُراجَعة (باستثناء #4 إجراءات جماعية، #5 سجل تدقيق مرئي، #10 جدولة نشر،
#11 صلاحيات متدرّجة) منجَزة الآن. الأربعة المتبقية بانتظار طلب صريح من المستخدم للبدء.

---

## [التاريخ: 2026-08-02 — جلسة 21: تنفيذ الموجة الأولى من مراجعة خبير للوحة التحكم (اقتراحات #1/#2/#3/#6)]

### السياق
مستخدم استعرض تحليلاً من "خبير" خارجي لعشر تحسينات محتملة بلوحة التحكم. راجعت كل نقطة ضد الكود
الفعلي (`AdminHome.jsx`, `PublishPanel.jsx`, `AdminRequestsQueue.jsx`) قبل أي تعليق — كل الاقتراحات
دقيقة الوصف. اقترحت ترتيب أولوية (٣←١←٦←٢ كموجة أولى منخفضة المخاطر)، ووافق المستخدم على البدء.

### ما تم إنجازه
1. **#3 شارة عدد الطلبات المعلَّقة**: `src/hooks/usePendingRequestsCount.js` (جديد) — نسخة عدّ فقط
   من منطق `fetchPendingPRs` بـ `AdminRequestsQueue.jsx` (PRs مفتوحة بعنوان `[event]`/`[upload]`).
   شارة حمراء بجانب رابط "طلبات الطلاب" بـ `AdminHome.jsx`، تختفي كلياً (لا صفر) لو بلا توكن/فشل جلب.
2. **#1 بحث/فلترة قائمة المواد**: حقل بحث (بالاسم/الرمز/الرقم) + أزرار فلترة (الكل/ظاهر/مخفي) فوق
   القائمة بـ `AdminHome.jsx` — محلي بالكامل بلا أي طلب شبكة إضافي، يعكس فوراً أي تعديل معاينة محلي
   للإخفاء (`localOverrides`) أيضاً.
3. **#6 تحذير مغادرة الصفحة بتعديلات غير منشورة**: `src/hooks/useUnsavedChangesWarning.js` (جديد،
   عام) — `beforeunload` قياسي. رُبِط بثلاث شاشات:
   - `StudyPlanEditor.jsx`: أعاد استخدام متغيّر `changed` الموجود أصلاً (لا منطق مكرَّر).
   - `AdminSectionsManager.jsx`: بصمة `sections` وقت آخر تحميل فعلي (`initialSectionsRef`، تُحدَّث
     بالتحميل الأول وبتبديل الدكتور النشِط) مقارنة بالحالي + أي استبدال ملف معلَّق.
   - `SubjectForm.jsx`: بصمة نصية لكل الحقول المحتوى الفعلي (لا الحقول المساعدة) عبر `useEffect`
     واحد، بلا تعديل أي معالِج `onChange` فردي على حدة (النموذج مجزَّأ لعشرات `useState` منفصلة).
     تُصفَّر عند نشر ناجح (`handlePublishSuccess`).
4. **#2 معاينة قبل النشر**: `describePackage(pkg)` جديدة بـ `PublishPanel.jsx` — تُشتَق من شكل `pkg`
   نفسه (بلا أي طلب شبكة إضافي) وتعرض "سيُكتب/سيُرفَع/سيُحذَف" قبل زر "نشر"، بتحذير أحمر صريح لحالة
   الحذف النهائي.

### 🔴 خللان حقيقيان اكتُشِفا بالصدفة أثناء تنفيذ #2 (غير مذكورين بمراجعة الخبير، أصلحتهما فوراً)
أثناء بناء `describePackage`، لاحظت أن الكود القديم بـ `PublishPanel.jsx` كان يفترض دائماً شكل
`buildSubjectPackage` حصراً (`pkg.subjectJson.name`, `pkg.pdfFiles.length`) بصرف النظر عن `pkg.kind`
الفعلي. تحقّقت من الثلاث نقاط استدعاء الحقيقية لـ `<PublishPanel>` وتأكَّدت:
1. **سطر العنوان** (`{pkg.subjectJson.name}...`): كان سيرمي `TypeError` فوراً بمجرد وصول `pkg` من
   `AdminHome.jsx` (حذف مادة) أو `StudyPlanEditor.jsx` (تحديث خطة المواد) — أي أن **شاشتَي "حذف
   مادة نهائياً" و"تحرير خطة المواد" كانتا معطَّلتين كلياً** بمجرد الوصول لمرحلة النشر (`PublishPanel`
   لا يُعرَض إطلاقاً، الصفحة تكسر بخطأ JS). أُصلِح بالتفرّع حسب `pkg.kind` بدل الافتراض الأعمى.
2. **زر "تنزيل حزمة (ZIP)"**: `exportPackageAsZip` بـ `lib/githubPublisher.js` (`for (const pdf of
   pkg.pdfFiles)` بلا حارس) كان سيرمي نفس نوع الخطأ لو ضُغط مع pkg حذف/تحديث خطة. أُصلِح بمستوى
   الواجهة فقط (لا تعديل على ملف عضو 5): الزر يُخفى كلياً (`canZip = !pkg.kind`) بدل تعطيله بلا
   تفسير — لا معنى له أصلاً بهاتين الحالتين (لا ملفات فعلية لتُجمَّع بـ ZIP).

### تحقّق مستقل
`npm install` (95 حزمة، مطابقة لـ`package-lock.json`) + `vite build` فعلي حتى النهاية — بناء ناجح،
94 وحدة، بلا أي خطأ صياغة/استيراد. تحقّقت يدوياً من كل استيراد جديد لـhooks (`useUnsavedChangesWarning`,
`usePendingRequestsCount`) بكل الملفات الثلاث/الاثنين المستهلِكة.

### توجيه للفريق
الموجة الثانية (اقتراحات #7 فاحص روابط مكسورة، #8 نسخة احتياطية كاملة، #9 لوحة إحصائيات) بانتظار
موافقة المستخدم على البدء — لم تُنفَّذ هذي الجلسة.

---

## [التاريخ: 2026-08-02 — جلسة 20: تشخيص وإصلاح "صفحة بيضاء عند الرجوع للخلف بدون إنترنت"]

### السيناريو المُبلَّغ عنه (حرفياً من المستخدم)
طالب يفتح صفحة مادة، يقطع الإنترنت، يعيد تحميل الصفحة (تعمل بنجاح بدون نت — هذا الجزء سليم أصلاً)،

ثم يضغط زر الرجوع بالمتصفح → **صفحة بيضاء تماماً**، يضطر لإعادة تحميل يدوي ليظهر له صفحة المواد
(الصفحة السابقة) بشكل صحيح.

### التشخيص
بلا وصول لمتصفح فعلي/أدوات مطوّر حيّة، لا يمكن الجزم بسبب واحد قاطع 100%. حدَّدت **ثلاث نقاط ضعف
حقيقية** بالكود الحالي، كلها قابلة للمساهمة بنفس العرَض، وأصلحت الثلاث معاً (دفاع متعدد الطبقات
بدل المراهنة على تشخيص واحد):

1. **🔴 خلل حقيقي مؤكَّد بالكود**: `lib/curriculum.js` — دالة `fetchFlatCurriculum()` كانت بلا
   `try/catch` حول `fetch()`. عند الرجوع بدون نت لصفحة "/" (`SubjectList.jsx`)، لو
   `curriculum.json` لم يُخزَّن بالكاش بعد (مثال: الطالب دخل مباشرة لرابط مادة، لم يزر "/" وهو
   متصل)، فشل الشبكة يرمي استثناءً غير مُمسوك — و`SubjectList.jsx` لا يستخدم `.catch()` على هذا
   الاستدعاء. **أُصلِح**: نفس نمط `useStudyPlan.js` تماماً الآن (فشل = مصفوفة فارغة، لا استثناء أبداً).
2. **🟡 فجوة تصميم محتملة**: الغلاف المخزَّن (`APP_SHELL_KEY` بـ`sw.js`) مستند HTML يشير لأسماء
   ملفات JS/CSS محدَّدة بـhash — لكن تلك الملفات كانت تُخزَّن فقط "عضوياً" (لو الطالب فعلاً طلبها
   أثناء تصفّحه)، بلا ضمان تطابق مع الغلاف المخزَّن بكل الأحوال. **أُصلِح**: `cacheShellAndAssets()`
   جديدة بـ`sw.js` تفحص نص الغلاف كل مرة يُخزَّن وتجلب/تخزّن كل `<script>`/`<link>` مذكور بداخله
   صراحة — الغلاف وأصوله يُخزَّنون دائماً كمجموعة متطابقة واحدة. رُفِع `CACHE_NAME` لـ`v3` لإبطال
   الكاش القديم (زُومِن `SW_CACHE_NAME` بـ`offlineSubjectPrefetch.js` معه، حسب التوثيق هناك).
3. **🟢 لا شبكة أمان عامة إطلاقاً (السبب الجذري للمشكلة الظاهرة، بصرف النظر عن 1/2)**: أي استثناء
   غير مُمسوك أثناء العرض (أياً كان مصدره — بما فيها أخطاء مستقبلية لم تُكتشف بعد) يوقف React عن
   عرض أي شيء بالكامل: هذا هو "الشاشة البيضاء" حرفياً، السلوك الافتراضي لـReact بلا حاجز أخطاء.
   **أُصلِح بطبقتين**:
   - `components/ErrorBoundary.jsx` (جديد): يغلّف كل التطبيق بـ`main.jsx` (خارج `BrowserRouter`
     عمداً). أي استثناء أثناء render → رسالة واضحة + زر "🔄 إعادة المحاولة" بضغطة واحدة، بدل شاشة
     بيضاء صامتة. أنماط inline صراحة (لا Tailwind) — لو سبب الخطأ فشل تحميل CSS نفسه، الرسالة تبقى
     مقروءة بمعزل تام عن ذلك.
   - `index.html`: شبكة أمان أخيرة بسكربت خام (بلا أي اعتماد على React) — لو `#root` ما زال فارغاً
     بعد 6 ثوانٍ (يغطي حالة فشل تحميل حزمة JS *نفسها* من الأساس، حيث ErrorBoundary لا يقدر يتدخّل
     لأنه هو نفسه جزء ممّا لم يُحمَّل)، تظهر نفس رسالة "إعادة المحاولة" تلقائياً.

### لماذا دفاع بثلاث طبقات بدل تشخيص واحد قاطع
بيئتي بلا وصول لمتصفح فعلي (بلا اتصال شبكة بالكامل)، فلا أقدر أُعيد إنتاج العرَض المضبوط بـ
DevTools/Network tab لأجزم بسبب واحد فقط. الإصلاح رقم 1 خلل مؤكَّد بالكود قطعاً (لا حاجة لإعادة
إنتاج لإثباته). رقم 2 يسد فجوة تصميم منطقية حقيقية بصرف النظر عن كونها السبب الدقيق هنا أم لا.
رقم 3 (الأهم عملياً): **يضمن أن أي فشل مستقبلي، مهما كان مصدره الدقيق وحتى لو لم أكتشفه اليوم،
يبقى قابلاً للتعافي بضغطة واحدة** — هذا يحل مباشرة الشكوى الفعلية للمستخدم ("يضطر لإعادة تحميل
يدوياً") بصرف النظر عن دقة تشخيصي للسبب الجذري الأصلي.

### التحقق (بلا `npm install`/متصفح فعلي — بيئتي بلا اتصال شبكة، نفس القيد التاريخي)
- فحص صياغة `esbuild` لكل ملف جديد/معدَّل (5 ملفات JS/JSX + `sw.js`): بلا أخطاء.
- تجميع كامل الشجرة من `src/main.jsx` (311.7kb): نجح بالكامل، بلا أي خطأ استيراد.
- **لم يُختبَر بمتصفح فعلي على الموقع المنشور** — الاختبار الحقيقي لنفس السيناريو المُبلَّغ عنه
  بالضبط (فتح مادة، قطع نت، تحديث، رجوع للخلف) **مطلوب من المستخدم** لتأكيد الإصلاح فعلياً، خصوصاً
  أن السبب الجذري الدقيق لم يُثبَت بيقين تام بدون أدوات مطوّر حيّة.

---

## [التاريخ: 2026-08-01 — جلسة 19: تطوير طريقة حفظ الصفحات بدون إنترنت]

### السياق
طلب مباشر من المستخدم: "طوّر طريقة حفظ الصفحات بلا انترنت". فحصت الآلية الحالية
(`offlineSubjectPrefetch.js` من جلسة 16، تُستدعى فقط ضمنياً من `FavoritesSection.jsx`) وحدَّدت
فجوتين حقيقيتين:
1. **لا تحكّم يدوي**: الحفظ مربوط حصراً بالتثبيت/المفضلة — طالب يريد حفظ مادة واحدة مهمة بدون
   تثبيتها دائماً لم يقدر.
2. **لا رؤية إطلاقاً**: الحفظ صامت بالكامل (لا أثر مرئي بأي مكان بالواجهة) — الطالب لا يعرف هل
   صفحة معيّنة محفوظة فعلاً بدون نت أم لا، قبل أن يجرّب فعلياً بدون اتصال.

### ما تم إنجازه
- **`lib/offlineSubjectPrefetch.js`**: أُضيفت طبقة تتبّع صريحة (`localStorage` + `CustomEvent`، نفس
  نمط `useFavorites.js`) فوق آلية الجلب الأصلية بلا تغيير بجوهرها. دوال جديدة:
  `listSavedSubjectPages`, `isSubjectPageSaved`, `removeSavedSubjectPage` (تحذف أيضاً إدخالات الكاش
  الفعلية بأفضل جهد). `prefetchSubjectForOffline` صار يرجّع `true/false` بدل لا شيء، ويسجّل بالقائمة
  عند النجاح فقط.
- **`hooks/useOfflineSubjectPages.js`** (جديد): عقد استهلاك موحَّد (`savedPages`, `isSaved`,
  `savePage`, `removePage`) لـ `Subject.jsx` و`OfflineDownloads.jsx`.
- **`pages/Subject.jsx`**: زر جديد "📥 حفظ هذه الصفحة بدون إنترنت" بنفس صف "اقترح حدثاً/اطلب رفع
  ملف" — يعمل لأي مادة بصرف النظر عن التثبيت، برسالة نجاح/فشل واضحة، وحالة "📥 محفوظة — تحديث" لو
  محفوظة مسبقاً.
- **`pages/OfflineDownloads.jsx`**: قسم جديد "📄 صفحات المواد المحفوظة" أعلى قسم الملفات الموجود
  (تحديث/إزالة لكل مادة)، مع تحديث التعليق التمهيدي القديم اللي كان لسه يذكر "لا Service Worker
  بهذي المرحلة" رغم وجوده فعلياً من جلسة 17 (تصحيح توثيقي بحت، بلا تغيير سلوك).

### قرارات تعمّدت عدم تنفيذها (ولماذا)
- **لم أوسّع الحفظ التلقائي لـ"آخر الزيارات" (`RecentlyViewedSection.jsx`)**: أي مادة تُزار وهي
  متصلة بالفعل تُخزَّن تلقائياً بكاش `sw.js` (استراتيجية "شبكة أولاً" العامة) — إضافة `prefetch`
  إجباري هنا كانت ستكرر نفس الطلبات بلا فائدة حقيقية (طلب شبكة إضافي لمحتوى مجلوب للتو أصلاً).
- **لم ألمس `public/sw.js`**: يعمل بشكل صحيح (نسخة v2 مُصلَحة فعلياً من جلسة سابقة)، لا علاقة
  مباشرة له بهذي المهمة — طبقة التتبّع الجديدة تمر عبره كما هي بلا أي تعديل مطلوب.

### التحقق (بلا `npm install` — بيئتي بلا اتصال شبكة، نفس القيد التاريخي)
- فحص صياغة `esbuild` للملفات الأربعة الجديدة/المعدَّلة: بلا أخطاء.
- تجميع كامل الشجرة من `src/main.jsx` (309.4kb): نجح بالكامل، كل مسارات الاستيراد الجديدة تُحل
  صحيحاً. تحذيرات `import.meta` فقط (متوقعة بصيغة iife، لا تؤثر بـ esm الفعلي).
- **لم يُختبَر بمتصفح فعلي** — خصوصاً `removeSavedSubjectPage` (حذف كاش فعلي عبر `caches.delete`)
  يحتاج تأكيداً بشرياً إنه يعمل صحيحاً على المتصفحات المستهدَفة.

---

## [التاريخ: 2026-08-01 — جلسة 18: تثبيت المواد أعلى القائمة + توثيق حادثة نسخة قديمة]

### حادثة يجب توثيقها بصراحة
الجلسة السابقة (بمحادثة أخرى مع المستخدم) بُنيت فوق حزمة zip **أقدم بكثير** من حالة هذا الريبو
الفعلية (تعادل تقريباً حالة ما قبل جلسة 13) — ناقصة: `sanitizeFileName`/ASCII بأسماء الملفات،
حفظ ترتيب الأقسام عند النشر، إدارة عناصر متقدمة بـ`SectionsManager`، `useNotifySubscriptions.js`،
`description`، تفصيل دكتور نظري/عملي، `scheduleDays.js`، `formatFileSize.js` — كلها **موجودة
وسليمة فعلياً بهذي الحزمة** (تحقّقت بـ`grep` مباشر لكل واحدة قبل أي تعديل، لا افتراضاً). أحد
القائمين على المشروع رصد هذا صح تماماً بمراجعة يدوية دقيقة. **لم يُحذَف أي شيء من طرفي** — أنا لا
أملك وصولاً لريبو GitHub الفعلي إطلاقاً؛ كل معرفتي بالمشروع مقصورة حصراً على آخر zip يُرفَع لي بكل
محادثة. البناء فوق نسخة قديمة بغير علمي أنتج انطباعاً بفقدان ميزات لم أفقدها أنا فعلياً، بل لم تصلني
من الأساس. **درس مهم يُطبَّق من الآن**: أي إدخال بسجلي لا يعني بالضرورة إنه "آخر حالة الريبو
الحقيقية" — لازم تأكيد المستخدم الصريح إنه يرفع نسخة مطابقة لآخر commit فعلي، خصوصاً بعد أي فجوة
زمنية أو تبديل محادثة.

**ملاحظة إيجابية بهذي الحزمة تحديداً**: تبيّن أنها متقدّمة أكثر مما توقعت أصلاً — تحتوي ميزة
"تصفح بدون إنترنت" (جلسة 16/17) بنسخة **أنضج** من التي بنيتها بالمحادثة السابقة (تتبّع تقدّم
التنزيل الحي، جلب استباقي لمواد المفضلة، فتح النسخة المحلية من زر "فتح بتبويب جديد" نفسه)، و`sw.js`
بنسخة مُصلَحة فعلياً (v2) لعطل حقيقي كان سيسبب بالضبط نفس الشكوى اللي أبلغ فيها المستخدم بالمحادثة
السابقة ("الموقع لا يفتح إطلاقاً بدون نت") — السبب الجذري الموثَّق بتعليق الكود نفسه: GitHub Pages
يرجّع محتوى `404.html` لأي مسار SPA فرعي لكن بحالة HTTP حرفية `404`، وشرط `response.ok` بالنسخة
الأولى كان يرفض تخزين أي رد بهذي الحالة — فمسارات مثل `/subject/computer-skills` لم تُخزَّن بالكاش
إطلاقاً رغم زيارتها بنجاح. **لم ألمس `sw.js` أو أي ملف من ميزة "بدون إنترنت" الحالية بهذي الجلسة** —
تبدو صحيحة وأنضج مما كنت سأكتبه، ولا داعٍ لإعادة بنائها.

### المهمة الفعلية المنجَزة: تثبيت المواد أعلى صفحة القائمة
تحقّق أولاً: هذي الميزة كانت فعلاً غائبة تماماً بهذي الحزمة (لا أثر بـ`SubjectList.jsx` ولا بالسجل).
نفس القرار المتخذ بالمحادثة السابقة (أعدت استخدام `useFavorites.js` الموجود أصلاً بدل نظام موازٍ
جديد)، لكن هذي المرة طبَّقته على النسخة **الحقيقية** من `SubjectCard.jsx` — التي تحتوي فعلياً ميزة
"سؤال الإشعارات" (`askingNotify` + `useNotifySubscriptions.js`, جلسة 2026-07-29) لم أكن أعرف
بوجودها بالمحاولة السابقة. التعديل هنا محكم: فقط `aria-label`/`title` الزر تغيّرا، **بلا أي لمس**
لمنطق `handleToggleFavorite`/`answerNotify`/`askingNotify` — تحقّقت بـ`diff` صريح قبل الحفظ.

- `SubjectList.jsx`: قسم "📌 المواد المثبّتة" أعلى الصفحة (ترتيب: آخر ما ثُبِّت أولاً)، ثم "كل
  المواد" بالترتيب المعتاد أسفله (بلا تكرار). يظهر فقط بالعرض الافتراضي — بحث Fuse.js نشِط يبقي
  الترتيب بالتطابق فقط، بلا تمييز تثبيت (نفس استثناء ترتيب السنة/الفصل الموثَّق أصلاً بالملف).
- `SubjectCard.jsx`: تسمية الزر فقط، لا شيء آخر.

### التحقق (بلا `npm install` — بيئتي بلا اتصال شبكة، نفس القيد التاريخي)
- فحص `grep` مباشر لكل ميزة زعم أحد القائمين إنها مفقودة — كلها موجودة فعلياً (بعضها بأسماء حقول
  مختلفة عمّا ذكره: مثلاً `sectionProfessors.theory/lab` بدل `professorNamePractical`، و
  `existingSections`/`sectionsBySection` بدل `existingOrder` — نفس الوظيفة، تسمية مختلفة).
- `diff` صريح بين `SubjectCard.jsx` قبل/بعد تعديلي — تأكيد لا لمس لمنطق الإشعارات.
- فحص صياغة `esbuild` للملفين المعدَّلين، ثم تجميع كامل الشجرة من `src/main.jsx` (300.6kb): نجح
  بالكامل، بلا أي خطأ استيراد بأي ملف بالمشروع (بما فيها كل ملفات ميزة "بدون إنترنت" الموجودة أصلاً).
- **لم يُختبَر بمتصفح فعلي**.

### توجيه للمستخدم
تأكد رجاءً من دمج هذي الحزمة (أو تعديلَي `SubjectList.jsx`/`SubjectCard.jsx` تحديداً) بالريبو
الفعلي قبل أي رفعة قادمة — راجع الفقرة الأولى أعلاه لسبب أهمية هذا تحديداً بهذا المشروع.

---

## [التاريخ: 2026-07-30 — جلسة 17: صفحات المفضلة بدون إنترنت + فتح النسخة المحلية بتبويب + حجم الملف وتقدّم التنزيل]

### ما تم إنجازه
بطلب مباشر من المستخدم (تكملة مباشرة لجلسة 16 — "تصفح المواد بدون إنترنت")، ثلاث ميزات:

1. **صفحات المواد المفضّلة قابلة للعرض بدون إنترنت حتى لو لم تُزَر من قبل**:
   - `src/lib/offlineSubjectPrefetch.js` (جديد): `prefetchSubjectForOffline`/`prefetchFavoriteSubjectsOffline`
     — طلب `fetch()` عادي صريح لـ `subject.json` + ملف محاضرات الدكتور النشِط فقط (نفس ما
     `useSubjectData.js` يجلبه أصلاً)، يمرّ عبر `sw.js` الموجود (جلسة 16) فيُخزَّن تلقائياً بلا أي
     تعديل على `sw.js` نفسه.
   - `FavoritesSection.jsx` (عضو 2، مُعدَّل بمعرفة المدير): `useEffect` جديد يستدعي
     `prefetchFavoriteSubjectsOffline(favorites)` عند التحميل الأول (يغطي مواد مفضّلة من قبل هذي
     الميزة) وعند أي تغيير بالمفضلة (إضافة/حذف) — بصمت كلياً، بلا أي مؤشر تحميل للمستخدم.
   - **بالتصميم**: هذا يغطي فقط عنوان المادة + قائمة عناوين المحاضرات (نص/بيانات)، لا ملفات
     pdf/image الفعلية داخلها — تلك تبقى اختيارية تماماً عبر زر "⭳ تنزيل" لكل ملف على حدة (لا خلط
     بين الميزتين، تماماً كما طلب المستخدم بالتفريق بينهما).

2. **"↗ فتح بتبويب جديد" يفتح النسخة المحلية لو الملف محمّل**: `LectureItem.jsx` — كان رابط `<a
   href={src}>` عادياً بصرف النظر عن حالة التنزيل. صار زراً يستدعي `openOffline(fileId)` (نفس دالة
   `useOfflineFiles.js` المستخدَمة بـ `OfflineDownloads.jsx`) لو الملف محمّل — يعمل بلا اتصال تماماً.
   خطة بديلة صامتة لرابط الشبكة لو تعذّر الفتح المحلي (حالة نادرة: حُذف من تبويب آخر بالتزامن).

3. **حجم الملف + تقدّم التنزيل الحي**:
   - `useOfflineFiles.js`: `downloadFile(meta, src, onProgress?)` — قراءة تدريجية عبر
     `response.body.getReader()` بدل `res.blob()` المباشرة، مع `onProgress(loadedBytes,
     totalBytes)` لكل دفعة (`totalBytes` من رأس `Content-Length`). خطة بديلة كاملة لمتصفح بلا دعم
     `ReadableStream` على `body` (نادر): يرجع لـ `res.blob()` بلا شريط تقدّم حي لكن بلا كسر الوظيفة.
   - `LectureItem.jsx`: طلب `HEAD` صامت عند فتح العنصر (لو غير محمّل بعد) يقرأ `Content-Length`
     ويعرض "حجم الملف: ..." قبل التنزيل. أثناء التنزيل: شريط تقدّم + "تم تحميل X من Y" حيّان. بعد
     التنزيل: الحجم الفعلي المخزَّن فعلياً بـ IndexedDB (`sizeBytes`) يظهر بجانب شارة "📥 محمّلة" —
     بلا أي طلب شبكة إضافي بهذي الحالة.
   - `src/lib/formatFileSize.js` (جديد): استُخرجت منه دالة "كيلوبايت/ميغابايت" العربية التي كانت
     مكرَّرة حرفياً بـ `OfflineDownloads.jsx` فقط — الآن مصدر واحد يستخدمه الملفان معاً.
     `FileUploaderWidget.jsx` (لوحة الأدمن) له تنسيقه الإنجليزي الخاص عمداً، لم يُلمَس.

### تحقّق مستقل
- بيئتي هذي الجلسة معها اتصال شبكة كافٍ لتشغيل `npm install` (95 حزمة، تطابق `package-lock.json`
  الموجود بلا أي إضافة اعتماديات جديدة) و`vite build` فعلياً حتى النهاية — بناء ناجح كامل، 90 وحدة،
  بلا أي خطأ صياغة/استيراد بكل الملفات الجديدة/المعدَّلة. حذفت `node_modules`/`dist` بعد التحقق (لا
  داعٍ لتضمينها بالتسليم).

### مشكلات واجهتها
- لا شيء تقني. البيانات الحقيقية لأحجام الملفات تعتمد على GitHub Pages إرسال رأس `Content-Length`
  فعلياً لملفات `public/pdf/**` الثابتة — هذا سلوك افتراضي لأي استضافة ثابتة عادية، لم أحتَج لتأكيد
  إضافي من المستخدم لأنه غير قابل للتخصيص من طرفنا أصلاً (خادم GitHub Pages نفسه).

### قرارات اتخذتها (ولماذا)
- الجلب الاستباقي للمفضلة يقتصر على `subject.json` + ملف محاضرات الدكتور *النشِط* فقط، لا كل
  `professorVariants` الممكنة — الطالب يرى واحداً فقط أصلاً بصفحة المادة (`useSubjectData.js`)، فجلب
  الباقي كان سيهدر بيانات بلا أي فائدة معروضة فعلياً.
- "فتح بتبويب جديد" يحاول النسخة المحلية أولاً *فقط* لو `downloaded` صراحة (لا محاولة عمياء)، مع
  رجوع صامت لرابط الشبكة عند الفشل — يحافظ على نفس السلوك بالضبط لأي ملف غير محمّل، توافق عكسي كامل.
- تتبّع التقدّم عبر stream reader يدوي بدل أي مكتبة تقدّم تنزيل خارجية (مثل axios/progress-stream)
  — لا اعتماديات جديدة بـ `package.json`، ونفس مبدأ `offlineFiles.js` بجلسة 16 (IndexedDB خام بلا
  مكتبة `idb`).

### توجيه للفريق
- لا عمل موزَّع جديد — نطاق محكم بعقود معروفة سلفاً من جلسة 16، نُفِّذ مباشرة بمعرفة المدير بطلب
  صريح من المستخدم، ضمن ملفات عضو 2 (`LectureItem.jsx`, `FavoritesSection.jsx`) وملفات جديدة كلياً
  بلا تعارض ملكية.

---

## [التاريخ: 2026-07-30 — جلسة 16: ميزة جديدة — تصفح المواد المحمّلة بدون إنترنت]

### ما تم إنجازه
بطلب مباشر من المستخدم (المهمة الأولى من `team-plan-downloads-notifications.md` المُقترَحة بجلسة
سابقة)، بنيت ميزة "تصفح بدون إنترنت" كاملة مباشرة (نطاق محكم، عقود معروفة سلفاً — لا حاجة توزيع
متوازٍ حقيقي):

1. **طبقة تخزين جديدة (`src/lib/offlineFiles.js`)**: IndexedDB خام (بلا مكتبة `idb` — لا اعتماديات
   جديدة بـ `package.json`، استثناء `jszip` الموثَّق بجلسة 1 يبقى الوحيد). قاعدة
   `assistant404-offline` > مخزن `files` (keyPath: `fileId`)، حدث مزامنة `CustomEvent` بنفس نمط
   `useFavorites.js` الحالي.
2. **`src/hooks/useOfflineFiles.js`** (جديد): عقد استهلاك موحَّد — `isDownloaded`, `downloadFile`,
   `openOffline`, `removeOffline`, `groupedBySubject`. بيانات وصفية فقط (بلا Blob) بالحالة
   المشتركة؛ الـ Blob يُجلَب فقط لحظة الفتح الفعلي (`getOfflineFile` داخلياً).
3. **`LectureItem.jsx` (عضو 2)**: props اختيارية جديدة (`fileId, subjectId, subjectName,
   sectionLabel`) — توافق عكسي كامل لأي استخدام مستقبلي بلا هذي القيم. زر "تنزيل" صار:
   - أول مرة: يجلب الملف، يخزّنه محلياً + يشغّل تنزيل المتصفح العادي بنفس اللحظة (نفس Blob).
   - محمّل مسبقاً: **لا استبدال صامت** — تأكيد صريح "فتح النسخة المحفوظة" مقابل "تنزيل مرة أخرى"
     (بالضبط طلب المستخدم الحرفي).
   - شارة "📥 محمّلة" بجانب العنوان دائماً الظهور (حتى بالحالة المطوية) — العلامة المطلوبة بصفحة
     المادة نفسها.
4. **`Subject.jsx` (عضو 2)**: يمرّر الآن `fileId` (`${subjectId}::${item.file}`) + `subjectId` +
   `subjectName` + `sectionLabel` لكل `LectureItem` من نوع pdf/image — بلا أي حالة/منطق إضافي هنا،
   كل شيء داخل `LectureItem.jsx` حصراً.
5. **`src/pages/OfflineDownloads.jsx`** (جديد): صفحة عامة `/offline` تعرض كل الملفات المحفوظة
   مقسّمة حسب المادة (فتح/حذف لكل ملف)، حالة فارغة واضحة، حجم إجمالي تقريبي.
6. **`App.jsx`/`Sidebar.jsx` (ملفات المدير)**: تسجيل مسار `/offline` + رابط دائم "📥 المواد بدون
   إنترنت" بالقائمة الجانبية — **ثابت الظهور دائماً** (بخلاف قسم "المفضلة" الشرطي)، لأنه صفحة ميزة
   أساسية لا تخصيص شخصي.

### تحقّق مستقل (بلا `npm install` — بيئتي بلا اتصال شبكة، نفس القيد الموثَّق بجلسة 1/2)
- فحص صياغة JSX/JS لكل الملفات الجديدة/المعدَّلة عبر `esbuild` محلي متاح بالبيئة (`--bundle=false`
  لكل ملف على حدة): بلا أي خطأ صياغة.
- تجميع كامل الشجرة فعلياً من `src/main.jsx` عبر `esbuild --bundle` (React/react-router/fuse.js/
  jszip بصفتها external): نجح بالكامل (294.6kb)، **كل مسارات الاستيراد الجديدة تحلّ صحيحاً** —
  تحذيرات `import.meta` فقط (متوقعة بصيغة iife لهذا الفحص، لا تؤثر بصيغة esm الفعلية لـ Vite).
- مراجعة يدوية سطراً بسطر لتدفّق التنزيل الأول/التنزيل المتكرر/الفتح المحلي بـ `LectureItem.jsx`.
- **لم أشغّل متصفحاً فعلياً** (نفس القيد التاريخي الموثَّق بجلسة 1/2) — التحقق البصري النهائي (شكل
  الشارة، سلوك زر التأكيد على الهاتف تحديداً) يحتاج اختبار بشري فعلي قبل الاعتماد الكامل.

### قرارات اتخذتها (ولماذا)
- **لا Service Worker بهذي المرحلة**: يطابق القسم 0 من `team-plan-downloads-notifications.md` —
  "تصفح المواد المحمّلة" لا يحتاج فتح shell الموقع من الصفر بدون نت، فقط تخزين الملفات نفسها. أي
  طلب مستقبلي لفتح الموقع كاملاً من الصفر بدون أول اتصال يحتاج قراراً منفصلاً (Service Worker حقيقي).
- **قسم دائم بالقائمة الجانبية لا شرطي**: خلافاً لـ `FavoritesSection`/`RecentlyViewedSection` (تختفي
  عند الفراغ)، رابط `/offline` ثابت دائماً — طلب المستخدم كان صريحاً "قسم خاص بالقائمة الجانبية"،
  والصفحة نفسها تعرض حالة فارغة واضحة بدل الاختفاء (اكتشاف الميزة أهم من التخصيص هنا).
- **تنزيل المتصفح العادي يبقى يعمل دائماً معاً مع التخزين المحلي** (لا استبدال أحدهما بالآخر) — لا
  داعي لحرمان الطالب من نسخة على جهازه (خارج المتصفح) فقط لأنه يريد أيضاً تصفحها داخل الموقع.
- **بلا مكتبة `idb`**: IndexedDB الخام كافٍ تماماً لهذا الحجم من العمليات (CRUD بسيط + cursor واحد)،
  ولا داعٍ لاعتمادية جديدة بمشروع قاعدته "ملكية حصرية + عقود محسومة سلفاً".

### هل احتجت تعديل ملف خارج نطاق الميزة؟
- فقط `App.jsx`/`Sidebar.jsx` (ملفات المدير أصلاً، لا خرق ملكية) لتسجيل المسار والرابط، و
  `Subject.jsx`/`LectureItem.jsx` (عضو 2، ضمن نطاق الميزة مباشرة).

### توجيه للفريق
- المهمة الثانية من نفس الخطة (إشعارات تصل لجهاز الطالب فعلياً) لم تبدأ بعد — تحتاج القرار المعماري
  بالقسم 0 من `team-plan-downloads-notifications.md` (Firebase Cloud Messaging أو سيرفر Push خاص)
  قبل أي عمل فعلي، لأنها الوحيدة من الميزتين اللي تحتاج بنية خارج GitHub Pages.
- اختبار بشري فعلي على الهاتف مطلوب قبل الاعتماد الكامل (نفس القيد بكل الجلسات السابقة): تحديداً
  حجم مساحة IndexedDB المتاحة فعلياً على متصفحات الهاتف الشائعة، وسلوك زر "تنزيل مرة أخرى" بشاشة
  ضيقة.

---

## [التاريخ: 2026-07-29 — جلسة 15: تحقق من ادّعاء سابق (غير موثَّق) + إصلاح ترتيب الأقسام + إدارة أقسام كاملة]

### ما تم إنجازه
- المستخدم طلب ميزتين: (أ) تعديل/استبدال/إعادة ترتيب/تغيير قسم أي ملف، و(ب) فحص اقتباس أرسله من
  محادثة سابقة يدّعي أن خللاً بترتيب الأقسام أُصلِح وأن ميزات مشابهة بُنيت. **لم أعتمد الاقتباس
  إطلاقاً** — بحثت بكل ملفات `docs/logs/` و`manager-log.md` عن أي أثر له (لا شيء)، ثم فحصت الكود
  الفعلي بهذي الحزمة تحديداً مباشرة.
- 🔴 **تأكَّد الخلل فعلياً بالكود، لا بالادّعاء**: `buildSubjectPackage` كان يفرض ترتيب أقسام ثابت
  دائماً (`SECTION_ORDER`)، متجاهلاً أي إعادة ترتيب فعلها الآدمن — `AdminSectionsManager.jsx` نفسه
  كان يوثّق هذا القيد صراحة بتعليق وتحذير بالواجهة. **لا شيء من الميزات الأربعة المدَّعاة كان مبنياً
  فعلياً** بـ `SectionsManager.jsx` (فقط تعديل الاسم وترتيب الأقسام نفسها — لا ترتيب العناصر، لا نقل
  بين الأقسام، لا استبدال ملف).
- أصلحت الخلل جذرياً بـ `githubPublisher.js` (عضو 5) وبنيت الميزات الأربعة فعلياً بـ
  `SectionsManager.jsx`/`AdminSectionsManager.jsx` (عضو 3) — راجع `docs/data-schema.md` §21
  والتفاصيل الكاملة بسجلي عضو 3/5.
- تحقّق مستقل شامل: `npm install && npm run build` (85 وحدة، بدون أخطاء) + 4 سيناريوهات Node
  مباشرة ضد `buildSubjectPackage` الفعلي (ترتيب مخصَّص محفوظ، افتراضي لمادة جديدة، استبدال ملف
  ناجح بلا تغيير على lectures.json، رفض استبدال بنوع خاطئ).

### مشكلات واجهتها
- لا شيء تقني — التحدي الوحيد كان تجاهل نص الاقتباس المُقنِع ظاهرياً والاعتماد فقط على فحص الكود.

### قرارات اتخذتها (ولماذا)
- وثّقت بصراحة كاملة بـ `docs/data-schema.md` §21 أن الاقتباس المرسَل كان غير دقيق بالكامل لهذي
  الحزمة، بدل تجاهل الملاحظة أو قبولها بصمت — الشفافية هنا تمنع تكرار نفس سوء الفهم لاحقاً (عمل
  "أُنجِز" بمحادثة لكنه لم يصل الريبو الفعلي، نفس نمط قسم 15/16 التاريخي).
- بنيت الميزات الثلاث الناقصة (ترتيب عناصر، نقل بين أقسام، استبدال ملف) مباشرة بنفس الجلسة بدل
  خطة توزيع منفصلة — نطاق محكم بملفي عضو 3 فقط، وعقد `fileReplacements` الجديد بعضو 5 بسيط
  ومعزول تماماً عن أي منطق موجود.

### هل احتجت تعديل ملف خارج نطاق الأعضاء المعنيين؟
- لا. `githubPublisher.js` (عضو 5)، `SectionsManager.jsx`/`AdminSectionsManager.jsx` (عضو 3)،
  وتوثيق عضو 6 (`data-schema.md` §21) فقط.

### توجيه للفريق
- 🔧 قاعدة دائمة يجب تذكّرها: أي "تم إنجازه" مذكور بمحادثة (حتى لو مقتبَساً حرفياً وبتفاصيل مقنعة)
  **لا يُعتبَر موجوداً** حتى يظهر فعلياً بالكود المفحوص مباشرة + سجل موثَّق بـ `docs/logs/`. لا
  استثناء لهذي القاعدة مهما بدا النص موثوقاً.

---

## [التاريخ: 2026-07-29 — جلسة 14: تحكم الآدمن بالمواعيد + إصلاح فيضان الهاتف + إصلاح تسمية الملفات]

### ما تم إنجازه
بطلب مباشر من المستخدم (3 مشاكل/طلبات بنفس الرسالة)، عملت مباشرة عبر ملفات الأعضاء المعنيين:

1. **ميزة: تحكم الآدمن بأوقات المحاضرات (مواعيد متعددة بالأسبوع).** كان `scheduleDays.theory`/`.lab`
   نصاً حراً واحداً فقط، **بلا أي حقل بالنموذج يعدّله** — التعديل الوحيد كان بالكود/JSON مباشرة (نفس
   شكوى المستخدم بالضبط). أضفت:
   - `src/lib/scheduleDays.js` (جديد، ملف "جاهز" محايد): `WEEK_DAYS`، مُفكِّك نص قديم أفضل-جهد،
     مُطبِّع شامل (نص/مصفوفة/غياب)، ومنسِّق عرض.
   - **عضو 3** (`SubjectForm.jsx`): قسم جديد "أوقات المحاضرات" — صفوف مواعيد قابلة للإضافة/الحذف
     لكل من نظري/عملي (يوم + بداية + نهاية + مكان اختياري)، تدعم عدة مواعيد بالأسبوع لنفس النوع.
   - **عضو 5** (`githubPublisher.js`): `buildSubjectPackage` يقبل الآن نصاً (توافق عكسي) أو مصفوفة
     مواعيد هيكلية، بنفس نمط الحفظ التلقائي المعتمَد سابقاً (مع تصحيح فحص truthy لمصفوفة فارغة).
   - **عضو 2** (`Subject.jsx`): يعرض كل موعد كسطر مستقل بدل جملة واحدة.
2. **إصلاح: فيضان أفقي بصفحة المادة على الهاتف** (موثَّق بلقطة شاشة المستخدم لمادة اللغة الإنجليزية
   1). السبب الجذري: عنوان طويل بلا مسافات طبيعية داخل `<span>` بصف flex بلا `min-w-0`/`break-words`.
   أصلحته بـ `LectureItem.jsx` (الأنواع الثلاثة: pdf/image, link, note) + إصلاح دفاعي مماثل على
   عنوان المادة والنبذة (`Subject.jsx`) وعنوان بطاقة المادة (`SubjectCard.jsx`).
3. **إصلاح: منطق تسمية الملفات المرفوعة** — كان يُحوَّل عبر `transliterateToSlug` لمحارف لاتينية
   بدل الاسم الأصلي (شكوى المستخدم بالضبط). هذا **تراجع جزئي متعمَّد عن قاعدة §17** (لا يمس
   `id`/`professorId` — تبقيان ASCII كما كانتا): دالة جديدة `sanitizeFileName` بـ `idSlug.js` (عضو
   4، بلا أي تحويل حروف)، تستهلكها `githubPublisher.js` و`studentSubmission.js` (عضو 5) الآن بدل
   `transliterateToSlug` لبناء اسم الملف الفعلي. **اكتشفت أثناء المراجعة خللاً حقيقياً منفصلاً** كان
   يجعل هذا التراجع خطيراً بلا إصلاحه أولاً: نداءات GitHub Contents API الخمس (بين الملفين) كانت
   تُدرِج مسار الملف خام بلا `encodeURIComponent` بعنوان الرابط — على الأغلب هذا هو السبب الحقيقي
   وراء "تلف ترميز أحرف عربية" الذي بُنيت عليه قاعدة §17 أصلاً. أضفت `encodeGitHubPath()` وطبّقتها
   على الخمس نداءات — الآن التراجع آمن فعلياً لا مجرد مسموح به.

### تحقّق مستقل (لا افتراضي، بكل الحالات)
- `npm install && npm run build`: نجحا كاملاً (84 وحدة، بدون أي خطأ).
- `buildSubjectPackage`: 3 سيناريوهات description (من جلسة سابقة، لم تتأثر)، 3 سيناريوهات
  scheduleDays هيكلي جديدة (إنشاء/حفظ تلقائي عند عدم التمرير/مسح صريح بمصفوفة فارغة).
- `sanitizeFileName`/`transliterateToSlug`: تحقّق مباشر أن العربي يُحفَظ حرفياً بالأولى، والثانية
  (معرّفات النظام) بلا أي تغيير بسلوكها.
- `parseLegacyScheduleString`/`normalizeScheduleList`/`formatScheduleEntry`: round-trip سليم على
  نص قديم فعلي من بيانات البذرة.

### قرارات اتخذتها (ولماذا)
- نفّذت الثلاثة مباشرة بنفس الجلسة (لا خطة توزيع منفصلة) — نفس منطق جلسة 12: نطاق محكم، عقود
  معروفة مسبقاً، لا حاجة تنسيق متوازي حقيقي.
- وثّقت التراجع عن قاعدة §17 بقسم مستقل (§18 بـ `data-schema.md`) بدل تعديل §17 نفسه أو حذفه —
  الحفاظ على السجل التاريخي لسبب القاعدة الأصلية مهم، والتراجع الجزئي يحتاج تبريراً موازياً واضحاً
  لا استبدالاً صامتاً.

### هل احتجت تعديل ملف خارج نطاق الأعضاء المعنيين؟
- لا. التعديلات محصورة بملفات عضو 2، عضو 3، عضو 4، عضو 5، وتوثيق عضو 6 (`data-schema.md`) — لا
  مساس بـ `team-plan-v2.md`، `App.jsx`، أو أي ملف إدارة/بنية عامة أخرى.

### توجيه للفريق
- لا عمل إضافي مطلوب — الميزات الثلاث جاهزة للنشر الحي فوراً. لو ظهر أي عطل مستقبلي متعلق بأسماء
  ملفات/مسارات GitHub غير سليمة، راجع `encodeGitHubPath` أولاً (نقطة التحقق الموثَّقة بقسم 18).

---

## [التاريخ: 2026-07-28 — جلسة 13: إضافة أوقات/قاعات المحاضرات لكل مادة]

### ما تم إنجازه
- بطلب مباشر من المستخدم (جدول دوام أسبوعي كامل: يوم + توقيت + قاعة/مخبر لكل مادة)، أثريت حقل
  `scheduleDays` الموجود مسبقاً (كان يحمل اسم اليوم فقط، مثل `"الثلاثاء"`) ليشمل التوقيت والقاعة
  بنفس القيمة النصية (`"الثلاثاء 08:30 - 10:10 (ق 4)"`) — **بلا أي تعديل على الحقل نفسه أو على
  الكود** (`Subject.jsx` يعرض `scheduleDays.theory`/`.lab` كنص خام أصلاً، فأي طول/تفصيل إضافي
  يُعرَض تلقائياً بلا حاجة لأي تغيير بمكوّن العرض).
- طُبِّق على 17 مادة من أصل 20 — المطابقة كانت بالاسم الحرفي للمادة بجدول الدوام مقابل `name`
  بـ `subject.json`. 4 مواد لم تُمَس لعدم ورودها بالجدول إطلاقاً بأي صيغة: `arabic-language-1`
  (الجدول ذكر "عربي 2" فقط، لا "عربي 1")، `computer-skills`، `database` (مخفية أصلاً)،
  `engineering-workshops` (الجدول يذكر "رسم هندسي"/"مهارات الاتصال للمهندسين"/"أخلاقيات مهنة
  الهندسة" لنفس الدكتور، لكنها مواد مختلفة اسماً عن "ورشات هندسية" — نفس القرار المتَّخذ بجولة سابقة
  عند تحديد الدكاترة، حفاظاً على الاتساق).
- مواد "المواد العامة" بالجدول (انكليزي 1، انكليزي 2، عربي 2) كانت بسطر واحد جماعي بلا تفصيل لكل
  مادة على حدة — طُبِّق نفس التوقيت/القاعة على `english-language-1`, `technical-english-2`,
  `arabic-language-2` كلٌّ على حدة، بما إنها فعلياً بنفس الموعد الجماعي المذكور بالجدول (الاثنين
  08:30-15:30، ق 7).
- تحقّق مستقل: `python3 -c "json.load(...)"` على كل ملفات `subject.json` (JSON صالح)، ثم
  `npm run build` نجح كاملاً (نفس 83 وحدة، بلا تغيير بالعدد — متوقَّع، تعديل بيانات فقط بلا كود).

### مشكلات واجهتها
- لا شيء.

---

## [التاريخ: 2026-07-27 — جلسة 12: ميزة جديدة — نبذة/رسالة عن المادة (description)]

### ما تم إنجازه
- بطلب مباشر من المستخدم ("إضافة وصف للمقرر — نبذة أو رسالة خاصة بكل مادة")، نفّذت الميزة كاملة
  من طرف إلى طرف بجلسة واحدة عبر ملفات ثلاثة أعضاء مختلفين (بدل توزيعها كخطة منفصلة، لأنها صغيرة
  ومحكمة النطاق ولا تحتاج تنسيقاً متوازياً):
  - **عضو 5** (`githubPublisher.js`): حقل `description` جديد بـ `buildSubjectPackage`، بنفس نمط
    الحفظ التلقائي المستخدَم مسبقاً لـ `sectionProfessors`/`scheduleDays` بالضبط (يُحفَظ تلقائياً
    لو لم يُمرَّر، يُحذَف فقط بنص فارغ صريح) — تفادياً متعمَّداً لتكرار فخ `setActive` القديم.
  - **عضو 3** (`SubjectForm.jsx`): `textarea` جديد بقسم "بيانات المادة" لإدخال/تعديل النبذة.
  - **عضو 2** (`Subject.jsx`): عرض النبذة أعلى صفحة المادة (نص خام، `whitespace-pre-wrap`) لو
    موجودة، بعد بيانات الدكتور/الدوام مباشرة.
  - **عضو 6** (`docs/data-schema.md` §1): وثّقت شكل الحقل الجديد وقاعدة الحفظ التلقائي.
- تحقّق مستقل: `npm install && npm run build` نجحا كاملاً (83 وحدة، بدون أخطاء). شغّلت أيضاً 3
  سيناريوهات Node مباشرة ضد `buildSubjectPackage` الفعلي (وصف جديد بمسافات → يُقلَّم، تعديل بدون
  تمرير الحقل → يبقى القديم كما هو، مسح صريح بنص فارغ → الحقل يختفي كلياً) — الثلاثة نجحت.

### مشكلات واجهتها
- لا شيء.

### قرارات اتخذتها (ولماذا)
- نفّذتها مباشرة عبر ملفات الأعضاء الثلاثة بنفسي بدل فتح خطة توزيع منفصلة (كما حصل مع ميزة
  "حذف مادة/تحرير خطة المواد" الأكبر حجماً بجلسة سابقة) — الفرق الجوهري: هذي ميزة حقل بيانات واحد
  بمسار كتابة/عرض معروف تماماً مسبقاً (لا عقد GitHub API جديد، لا حذف بيانات، لا مخاطر أمنية)،
  فتكلفة التنسيق المتوازي كانت أعلى من فائدته هنا.
- قيّدت النص صراحة كـ"خام فقط، بلا Markdown/HTML" لمطابقة نفس قيد عنصر `note` الموجود مسبقاً —
  توحيد السياسة بدل قاعدة موازية جديدة لحقل نصي حر آخر.

### هل احتجت تعديل ملف خارج نطاق الأعضاء المعنيين؟
- لا. التعديلات محصورة بالضبط بملفات عضو 2 (`Subject.jsx`)، عضو 3 (`SubjectForm.jsx`)، عضو 5
  (`githubPublisher.js`)، وتوثيق عضو 6 (`data-schema.md`) — لا مساس بأي ملف آخر.

### توجيه للفريق
- لا عمل إضافي مطلوب — الميزة جاهزة للنشر الحي فوراً (نموذج → بناء الحزمة → PR → صفحة المادة).

---

## [التاريخ: 2026-07-27 — جلسة 11: انتقال المشروع لحساب جيتهاب جديد بالكامل]

### ما تم إنجازه
- المستخدم بدأ نشر المشروع من حساب/ريبو جديد بالكامل: **`Omar-218/QPU_assistant_404`** (بديل نهائي عن `Omar-218`).
- مسحت كل أثر للحساب القديم من كامل المشروع: `adminAuth.js` (`DEFAULT_OWNER`)، `studentSubmission.js` (`OWNER`)، وكل التوثيق (`docs/manager-log.md` نفسه، `docs/team-plan-v2.md`، `docs/data-schema.md`، كل سجلات الأعضاء) — استبدال شامل بلا استثناء.
- ⚠️ ملاحظة منهجية: هذا استثناء متعمَّد من قاعدة "لا تُحذف/تُعدَّل الإدخالات القديمة" بسجلات الأعضاء والمدير — بُرِّر بطلب صريح من المستخدم لمسح أي ربط بالحساب القديم، لا كإجراء روتيني. لن يتكرر إلا بطلب مماثل صريح مستقبلاً.
- تحققت: لا تسريب توكن فعلي بأي ملف (المطابقات الوحيدة لنمط `github_pat_` كانت placeholder نصي بحقول الإدخال، لا قيمة حقيقية).

### قرارات اتخذتها (ولماذا)
- لم ألمس `vite.config.js` (مسار base) — اسم الريبو نفسه لم يتغيّر (`QPU_assistant_404`)، فقط المالك، والـ base path لا يتضمّن اسم المالك إطلاقاً.
- لم ألمس مرجع "4cml/assistant404" بـ`StudyPlan.jsx` (مشروع v1 الأصلي المصدر لبيانات الخطة الدراسية) — كيان مختلف تماماً غير مرتبط بحساب الاستضافة الحالي، لا علاقة له بـ"الحساب القديم" موضوع الطلب.

### توجيه للفريق / للمستخدم مباشرة
راجع القائمة الكاملة بالرد المرافق لهذا الإدخال — أهمها: توكن الأدمن، توكن الطالب المقيَّد (يحتاج حساب Collaborator جديد على الريبو الجديد أيضاً)، الـ Secret بإعدادات Actions، وحماية فرع main بالريبو الجديد — كلها إعدادات حساب/ريبو يجب إعادتها من الصفر، الكود وحده لا يكفي.

---


### ما تم إنجازه
- لقطة شبكة من المستخدم للصفحة الرئيسية كشفت **3 طلبات متزامنة** لـ`study-plan.json` بتحميل واحد
  (`SubjectList.jsx` + `FavoritesSection.jsx` + `RecentlyViewedSection.jsx` — الثلاثة مُعرَضة معاً
  دائماً، كل واحد يستدعي `useStudyPlan()` مستقلاً). نفس فئة الخلل اللي أصلحه عضو 4 بـ
  `useEvents.js`/`useUploadRequestsLog.js` بالجلسة السابقة، بس بملف ملكيته نفسه لم يُطبَّق عليه بعد.
- طبّقت أنا مباشرة (ملف عضو 4، تعديل استثنائي موثَّق بالملف نفسه) نفس نمط التجميع بوعد مشترك
  (`inflight promise`) على `useStudyPlan.js` — بلا تخزين مؤقت دائم عمداً، لنفس السبب (طازجية بعد
  نشر مادة جديدة).

### قرارات اتخذتها (ولماذا)
- طبّقتها مباشرة بدل الانتظار لتسليم عضو 4 — الفحص ونمط الحل معروفان ومختبَران مسبقاً بملفين
  شقيقين بنفس الجلسة الماضية، فالمخاطرة منخفضة جداً وتأخير إصلاح بهذا الوضوح غير مبرَّر.

### توجيه للفريق
- عضو 4: يُستحسن مراجعة هذا التعديل وتسجيله بسجلّه الخاص لضمان التماسك، ومسح بقية ملفاته
  (لو فيه أي هوك ثاني بنفس النمط القديم) للتأكد ما تكرر نفس الخلل بمكان لم يُكتشَف بعد.

---


### ما تم إنجازه
- قارنت أحدث نسخة حية من المشروع (`QPU_assistant_404-essential.zip`) بآخر نسخة اعتمدتها — تأكدت
  أولاً إن تصحيح اسم المالك (`Omar-218`، جلسة سابقة) وصل فعلاً لكل من `adminAuth.js` و
  `studentSubmission.js` بالنسخة الحية. رصدت تغييرات مشروعة أخرى حصلت مباشرة (بلا دخل مني):
  - `LectureItem.jsx`/`Subject.jsx`: استبدال عارض ملء الشاشة (`FileViewer.jsx`، حُذف كاملاً) بقائمة
    "تنزيل/فتح بتبويب جديد" بسيطة — موثَّق بتعليق يشرح السبب (إيهام مستخدم الجوال إن الملف صفحة
    وحدة بسبب الـ iframe). فحصت: لا استيراد ميت متبقٍّ لـ`FileViewer` بأي مكان. سليم.
  - `ContactSection.jsx`: أرقام واتساب/تيليجرام حقيقية اتُّبعت، خيار الاتصال المباشر أُزيل — تعديل
    بيانات بحت، لا كود.
  - مواد جديدة نُشرت فعلياً (`computer-architecture`, `math-2`, `programming-2`) — دليل إضافي إن
    خط نشر المواد يعمل بشكل صحيح حياً بعد تصحيح اسم المالك.
- راجعت حزمة "dedup-fix-patch" من عضو 4 (إصلاح تكرار طلب `events.json`/`upload-requests-log.json`
  اللي رصدته أنا من لقطات الشبكة بجلسة سابقة). **جودة ممتازة، بلا أي ملاحظات**: تشخيص دقيق (الهيدر
  وصفحة المادة يستدعيان `useEvents()` كل واحد مستقلاً)، إصلاح صحيح (تجميع الطلبات المتزامنة بوعد
  مشترك يُفرَّغ فور الاكتمال — عمداً بلا تخزين مؤقت دائم، لأن لوحتَي الأدمن تحتاجان بيانات طازجة بعد
  كل قرار)، مُختبَر فعلياً (محاكاة Node لاستدعاءين متزامنين + `npm run build` كامل عندهم)، وملتزم
  تماماً بنطاق ملكية عضو 4. دمجته مباشرة بلا أي تعديل.

### مشكلات واجهتها
- لا شيء تقني يُذكر — أول مرة تسليم عضو 4 يصل كاملاً وجاهزاً للدمج المباشر بلا أي إصلاح مطلوب مني.

### قرارات اتخذتها (ولماذا)
- اعتمدت نسخة "essential.zip" الحية كخط أساس جديد كامل (بدل الاستمرار من نسختي القديمة) — تحتوي
  تعديلات مشروعة حصلت مباشرة (FileViewer، ContactSection، مواد جديدة) لا داعي لإعادة تطبيقها يدوياً.

### توجيه للفريق
- لا عمل جديد مطلوب — الدفعة 5 مكتملة وظيفياً ومُحسَّنة أداءً. بانتظار تفعيل خطوتَي §0.1 (توكن
  الطالب المقيَّد + حماية فرع main) من المستخدم مباشرة لأول اختبار حي كامل للتدفق.

---


### ما تم إنجازه
- راجعت 4 حزم تسليم (عضو 2، عضو 3، عضو 4، عضو 5) لخطة الدفعة 5 ودمجتها بنسختي المعتمَدة.
- **عضو 4 لم يسلّم شيئاً بهذي الدفعة إطلاقاً** — حزمته كانت من 2026-07-21 (توثيق قديم من دفعة سابقة، لا علاقة لها بالأحداث/الإشعارات). كتبتُ أنا مباشرة الملفات الثلاثة الناقصة المفترَض تسليمها: `lib/eventStatus.js` (`isEventActive`)، `hooks/useEvents.js`، `hooks/useUploadRequestsLog.js`، `hooks/useNotifications.js` — بنفس التوقيع الحرفي اللي وثّقه عضو 2 وعضو 3 بتعليقات mock الخاصة بهم، فالتبديل كان بلا أي احتكاك.
- عضو 2 وعضو 5: جودة ممتازة، مطابقة كاملة لعقود خطة الدفعة 5 (عزل التوكن، توثيق mock دقيق بنقاط تبديل واضحة). لا ملاحظات تُذكر.
- 🔴 **اكتشفت ثغرة تكامل حرجة بتسليم عضو 3**: خطة عضو 3 كانت تفترض إعادة استخدام `PublishPanel.jsx` لمسار "قبول" طلب طالب بعد تسليم عضو 5 — لكن `PublishPanel` مبني حصراً حول `pkg` من `buildSubjectPackage` (يقرأ `pkg.subjectJson.name`/`pkg.pdfFiles.length` مباشرة، سيرمي خطأ فوراً بأي pkg آخر) ويستدعي `publishToGitHub`، بينما عضو 5 صمّم "قبول" ليمر حصراً عبر `mergeExistingPR` (تكتب على فرع الطالب الموجود ثم تدمج نفس الـ PR) — لا علاقة لها بـ `PublishPanel` إطلاقاً. لو طُبِّق "التبديل" كما خطّط له عضو 3 حرفياً، كان القبول سينكسر بالكامل (خطأ فوري) لكل طلب طالب.
  - أعدت كتابة `AdminRequestsQueue.jsx` بالكامل: قبول = `buildEventDecision`/`buildUploadDecision` (بعد جلب `existingEvents`/`existingUploadsLog`/`existingLectures` الفعلية من `main` + تحديد `lecturesFileName` الصحيح لو المادة متعددة الدكاترة) ثم `mergeExistingPR` مباشرة، بواجهة نتيجة مبسَّطة (لا PublishPanel إطلاقاً).
  - 🔴 اكتشفت ثغرة ثانية بنفس الملف أثناء الإصلاح: **الرفض كان يُقفل PR الطالب مباشرة بلا أي تسجيل بالسجل** — يخالف صراحة متطلب المستخدم الأصلي ("سجل يسجل هذا كله") وتوثيق عضو 5 نفسه (`events.json`/`upload-requests-log.json` يشملان `status: "rejected"`). أضفت خطوة تسجيل القرار (`publishToGitHub` بفرع/PR جديد منفصل تماماً عن فرع الطالب) **قبل** إقفال PR الطالب بـ `closePendingRequestPR` — الاثنان معاً فقط يشكّلان "رفض" كامل ومسجَّل.
- أضفت مسارات `/notifications`، `/admin/requests`، `/admin/events-log` بـ `App.jsx` (كانت غير مسجَّلة رغم وجود روابط لها بـ `AdminHome.jsx` من عضو 3 — كانت ستؤدي لصفحة 404).
- أضفت زر جرس بجانب زر الثيم بـ `Header.jsx` (مهمة المدير بجدول التوزيع) — يفتح `/notifications`، مع نقطة "جديد" محسوبة محلياً بـ `localStorage` فقط (بلا خادم)، تُصفَّر فور الضغط.

### مشكلات واجهتها
- بيئتي بلا اتصال شبكة ولا أدوات بناء (`vite`/`eslint`) — تحققت من ملفات JS الصرفة (`useEvents.js`, `eventStatus.js`, `studentSubmission.js`, `githubPublisher.js`, إلخ) بـ `node --check` (نجحت كلها)، وراجعت ملفات JSX يدوياً سطراً بسطر (لا أداة تفحص JSX هنا).
- لا يمكن اختبار أي مسار GitHub حي فعلي (فتح فرع/PR/دمج حقيقي) — ينتظر خطوتي §0.1 من خطة الدفعة 5 (توكن مقيَّد + حماية فرع `main`) المطلوبتين من المستخدم مباشرة، لم تُفعَّلا بعد حسب علمي.

### قرارات اتخذتها (ولماذا)
- كتبت hooks عضو 4 الناقصة بنفسي بدل إرجاع التسليم بالكامل — التوقيعات كانت موثَّقة بدقة كافية بتعليقات mock عضو 2/عضو 3 لبنائها بثقة، وتأخير الدفعة كاملة لغياب تسليم واحد بسيط كان أقل كفاءة من إنجازه مباشرة.
- لم أُبقِ أي نسخة من `PublishPanel.jsx` بمسار قبول طلبات الطلاب — فصلته بمنطق مخصَّص كاملاً بـ `AdminRequestsQueue.jsx` لتفادي أي التباس مستقبلي بين مسارَي "نشر مادة عادي" و"قبول طلب طالب" (شكلا pkg مختلفان تماماً).

### هل احتجت تعديل ملف خارج نطاق ملكية عضو ما؟
- نعم، بشكل استثنائي: `AdminRequestsQueue.jsx` (عضو 3) أُعيد كتابته بالكامل من طرفي لإصلاح الثغرتين أعلاه — موثَّق بتعليق صريح أعلى الملف يشرح السبب لعضو 3 عند مراجعته لاحقاً.

### توجيه للفريق
- **عضو 4**: مطلوب تسليم فعلي لهذي الدفعة (حتى لو متأخر) — الملفات الأربعة كُتبت بالنيابة عنه هذي المرة فقط، يُفضَّل مراجعتها والتعليق عليها بسجله الخاص لضمان التماسك مستقبلاً.
- **لا عمل جديد لباقي الأعضاء** — الدفعة مكتملة وظيفياً بانتظار خطوتي §0.1 من المستخدم مباشرة (توكن الطالب المقيَّد + حماية فرع main) قبل أي اختبار حي.

---


### ما تم إنجازه
- راجعت حزمة تعديلات مقترحة من عضو ("changed-files-patch") تدّعي إصلاح ميزة الحذف وتصحيح دكاترة إضافيين وأسماء أخرى — تبيّن بالمقارنة الحرفية ملفاً ملفاً إنها مبنية على نسخة قديمة سابقة حتى لجلسة 6: كل الأجزاء "الجديدة" فيها (الحذف، دفاعات ASCII، تصحيح بقية الدكاترة) كانت مطابقة 100% لما هو موجود أصلاً، والجزء الوحيد المختلف فعلياً (حل بديل لمشكلة دكتور العملي بحقل `professorNamePractical` داخل `professorVariants` بدل `sectionProfessors` على مستوى المادة) كان **يرجع خطأ "د. علي"** بمادة الدارات الكهربائية بدل إصلاحه رغم ادّعاء توثيقهم المرفق (`data-schema.md`, مؤرَّخ من جلسة التأسيس) إنه مُصلَح. **رفضت الحزمة كاملة** — المستخدم أكّد اعتماد نسختي الأخيرة (جلسة 6) كمرجع نهائي.
- أضفت قسم "تواصل مع المشرفين" مثبَّت دائماً أسفل القائمة الجانبية (`ContactSection.jsx` جديد، مملوك للمدير) — واتساب/تيليجرام/اتصال مباشر بأيقونات SVG بسيطة بتوكنز الثيم (بلا ألوان hex مباشرة، بلا مكتبة أيقونات خارجية جديدة). عدّلت `Sidebar.jsx` (مملوك للمدير أصلاً) ليصير المحتوى العلوي (روابط + مفضلة + آخر الزيارات) بمنطقة تمرير مستقلة (`flex-1 overflow-y-auto`) وقسم التواصل خارجها بالأسفل دائماً، بلا تغيير على سلوك الـ drawer بالجوال أو الوضع الثابت بالشاشات الكبيرة.

### مشكلات واجهتها
- بيانات التواصل الفعلية (أرقام واتساب/تيليجرام الحقيقية) غير متوفرة عندي — تركت قيماً بديلة واضحة (`963900000000`, `your_username`) بأعلى `ContactSection.jsx` مع تعليق يوضّح بالضبط أي سطر يُعدَّل وبأي صيغة، بلا حاجة للمس أي جزء آخر بالملف.

### قرارات اتخذتها (ولماذا)
- أيقونات SVG مرسومة يدوياً (شكل عام: فقاعة دردشة / سهم إرسال / سماعة هاتف) بدل استيراد مكتبة أيقونات جديدة — يتفادى استثناء ملكية/تبعية جديد بلا داعٍ لثلاث أيقونات بسيطة فقط.
- رفض حزمة العضو كاملة بدل دمج الأجزاء المفيدة منها يدوياً — كل ما فيها من قيمة كان مكرراً أصلاً، والجزء المختلف الوحيد كان تراجعاً فعلياً (رجوع خطأ الدارات)، فالدمج الجزئي كان سيضيف تعقيداً بلا أي فائدة صافية.

### توجيه للفريق
- 🔧 **مطلوب من المستخدم مباشرة (لا عضو)**: تعبئة أرقام/روابط واتساب وتيليجرام والهاتف الحقيقية بأعلى `src/components/layout/ContactSection.jsx` (3 أسطر معلَّمة بـ 🔧) قبل النشر النهائي.

---

## [التاريخ: 2026-07-24 — جلسة 6]

### ما تم إنجازه
- 🔴 راجعت تسليم عضو مكلَّف بتحديث أسماء دكاترة المواد وفق جدول رسمي أرسله المستخدم — اكتشفت الحزمة **غير منجزة بالكامل**:
  - مادة "الدارات الكهربائية - 1": الدكتور الثاني المُضاف اسمه **"د. علي"** (لا علاقة له بالمادة إطلاقاً حسب تأكيد المستخدم المباشر) بدل "م. سليمان محمد" الصحيح — وملف محاضراته `lectures-prof-ali.json` **غير موجود على القرص أصلاً**، فأي طالب يفتح تبويبه كان سيصطدم بخطأ تحميل فعلي.
  - 4 مواد (برمجة 1، الفيزياء 1، الإلكترونيات 1، تحليل النظم والإشارات) أُضيف لها دكتور النظري فقط، ودكتور العملي **نُسي كلياً** رغم وجوده صراحة بالجدول المرسَل.
- 🔴 بعد الإصلاح الأول، لاحظ المستخدم مشكلة أعمق: حتى لو أُضيف دكتور عملي عبر آلية `professorVariants` الحالية، صفحة المادة (`Subject.jsx`) كانت تعرض **دكتوراً واحداً فقط** ("الدكتور: X" — نص العرض نفسه بصيغة مفرد) بلا أي إشارة لتعدد الأدوار. فحصت السبب الجذري: `professorVariants` صُمّم أصلاً (جلسة 1) لحالة "شعبتين بمحتوى بديل كامل يُختار بينهما دكتور نشِط واحد فقط فيُعرض" — **وليس** لحالة "نظري + عملي بنفس المحتوى المشترك"، وهذا يفسّر لماذا `useSubjectData`/`professorVariants.js` يعرضان `activeProfessor` واحداً بالتصميم لا كخلل عرضي.
- صممت وأضفت حقلاً جديداً مستقلاً تماماً بعقد `subject.json`: **`sectionProfessors: { theory?, lab? }`** — تسمية عرض بحتة بلا أي علاقة بـ `professorVariants`/`lecturesFile` (موثَّق كاملاً أعلى `githubPublisher.js`). عدّلت 3 طبقات:
  - `buildSubjectPackage` (عضو 5): يقبل `subjectMeta.sectionProfessors`، يكتبه بـ `subjectJson` فقط لو فيه قيمة فعلية.
  - `SubjectForm.jsx` (عضو 3): قسم جديد بالنموذج — حقلي "دكتور النظري"/"دكتور العملي" مستقلَّين كلياً عن قسم "متعددة الدكاترة" الموجود، تعبئة مبدئية من `initialSubject.sectionProfessors` عند التعديل.
  - `Subject.jsx` (عضو 2): يعرض "نظري: X" و"عملي: Y" معاً لو أحدهما موجود، ويرجع للسطر القديم "الدكتور: X" تلقائياً لو الحقل الجديد فارغ بالكامل (توافق عكسي كامل مع كل المواد التي لم تُلمَس).
- أصلحت بيانات 5 مواد فعلياً: حذفت "د. علي" ومرجعه المكسور من `circuits/subject.json` نهائياً وعوّضته بـ `sectionProfessors.lab: "د. أحمد العريفي، م. سليمان محمد"`، وأضفت `sectionProfessors` الصحيح لـ `programming-1`، `physics-1`، `electronics-1`، `signals-and-systems` وفق الجدول الرسمي حرفياً.

### مشكلات واجهتها
- بيئتي بلا اتصال شبكة ولا `node_modules` مثبَّتة — ما قدرت أشغّل بناء/فحص صياغة آلي (`vite build`/`eslint`) على التعديلات. راجعت كل تعديل بـ `SubjectForm.jsx`/`Subject.jsx`/`githubPublisher.js` يدوياً سطراً بسطر (توازن أقواس JSX، امتداد مصفوفة اعتماديات `useMemo`) بدل الاعتماد على أداة.

### قرارات اتخذتها (ولماذا)
- `sectionProfessors` نص وصفي حر بلا بنية (يقبل أكثر من اسم بنفس الحقل، مثال "د. أحمد العريفي، م. سليمان محمد") بدل مصفوفة أسماء منظَّمة — لأن حالات الجدول الفعلية غير منتظمة (أحياناً دكتور واحد للعملي، أحياناً دكتوران معاً)، وبناء بنية صارمة الآن سابق لأوانه بلا حاجة فعلية مؤكَّدة لفرز/بحث بالاسم مستقبلاً.
- لم ألمس `professorVariants`/`multiProfessor` الموجودة إطلاقاً — الحقل الجديد طبقة عرض إضافية بمعزل تام، فلا خطر على أي مادة تستخدم `professorVariants` فعلياً لغرضها الأصلي (شعب بديلة).
- لم أطبّق `sectionProfessors` على "مبادئ البرمجة" رغم ذكرها بالجدول (نظري وعملي لنفس الدكتور) — السطر القديم "الدكتور: X" يغطي هذي الحالة بدقة كاملة بلا داعٍ لتكرار نفس الاسم بحقلين.

### توجيه للفريق
- لا عمل جديد موزَّع هذي الجلسة — كل الإصلاحات نُفِّذت مباشرة بملفات عضو 2/3/5 بمعرفة المدير بعد طلب صريح من المستخدم، ومتوافقة مع نطاق كل عضو الأصلي (لا خرق ملكية دائم — مجرد تصحيح بالنيابة).
- عند أي تسليم مستقبلي "يضيف دكتور عملي"، المسار الصحيح من الآن: حقلي "دكتور النظري/العملي" الجديدين بـ`SubjectForm.jsx`، لا محاولة إضافة `professorVariants` ثانٍ لنفس المحتوى (هذا كان سبب خطأ "د. علي" الأصلي على الأرجح — خلط بين الآليتين).

---



### ما تم إنجازه
- إعادة توزيع مهمة "أنواع محتوى المحاضرة" (pdf/image/link/note) بعد ملاحظة صحيحة من المستخدم: التوزيع الأول (بصيغة "راجعوا الملفات ذات العلاقة") كان يفرض على كل عضو مجهوداً غير ضروري بمراجعة ملفات لا يملكها.
- أعدت التوزيع بصيغة دقيقة: قائمة صريحة لكل عضو (يستلم/يسلّم) بلا أي احتياج مراجعة كود عضو آخر — العقد النصي وحده كافٍ للعمل المتوازي.
- حسمت تفاصيل العقد الحرجة اللي كانت غامضة باقتراح عضو 2: توافق عكسي لعنصر بلا `type` (= pdf دائماً)، اسم المجلد `public/pdf/{slug}/` يبقى كما هو حتى لـ `image`، منع Markdown/HTML بحقل `content` (قرار أمان)، منع SVG بالصور (نفس السبب)، تحقق `url` مزدوج (واجهة + خط أنابيب معاً).
- غيّرت اسم معامل `buildSubjectPackage` من `files` إلى `items` بالعقد الجديد (أدق تسمية بعد تعدد الأنواع) — عضو 5 يطبّقها بملفه فقط.

### مشكلات واجهتها
- لا شيء تقني — الملاحظة كانت تنظيمية بحتة من المستخدم، صححتها فوراً.

### قرارات اتخذتها (ولماذا)
- **عضو 4 هذي الجولة بلا أي ملف كود** — نطاقه المعلن أصلاً (`hiddenFilter.js`) لا يتأثر بـ `type` إطلاقاً (يشتغل على `hidden` بمعزل تام)، فتكليفه بمراجعة كود إضافي كان عبئاً بلا فائدة فعلية.
- عدم لمس `SectionsManager.jsx` (عضو 3) إلا لو ظهرت حاجة فعلية أثناء التنفيذ — القسم/الإخفاء بمعزل عن نوع المحتوى منطقياً.

### توجيه للفريق
- عضو 3، عضو 5، عضو 2 يبدأون بالتوازي الكامل الآن، كل واحد بملفاته المحدَّدة صراحة فقط.
- عضو 4 يسلّم نص توثيق فقط (لا كود)، يُلصَق لاحقاً بملف عضو 6.
- عضو 6 ينتظر تسليم 2/3/5 قبل ما يبدأ.

---

## [التاريخ: 2026-07-20 — جلسة 4]

### ما تم إنجازه
- راجعت مقترح عضو 1 لميزة "حذف مادة من لوحة التحكم مباشرة + تحرير خطة المواد" — اعتمدت التوزيع المقترح كاملاً.
- أضفت فجوة تقنية لم يذكرها عضو 1: حذف مجلد `pdf/{slug}` عبر GitHub API يحتاج حلقة `DELETE` لكل ملف على حدة (لا يوجد endpoint يحذف مجلداً دفعة وحدة)، وحذف مادة متعددة الدكاترة يحتاج حذف كل ملفات `lectures-{professorId}.json` المرتبطة، لا ملف واحد فقط.
- حسمت قاعدة أمان دائمة: **عمليات الحذف تفتح PR للمراجعة دائماً، بلا استثناء دمج تلقائي** — أول استثناء دائم من سياسة `autoMerge: true` الافتراضية (القرار الافتراضي يبقى للإضافة/التعديل فقط).
- وضّحت لعضو 3 الفرق بين "تحرير خطة المواد" (بيانات وصفية بالقائمة فقط) و`SubjectForm.jsx` الموجود (محتوى المادة الفعلي) لتفادي التباس بالنطاق.

### مشكلات واجهتها
- لا شيء.

### قرارات اتخذتها (ولماذا)
- الحذف عبر PR لا دمج تلقائي: الإضافة/التعديل قابلة للتراجع بسهولة، الحذف مدمّر وأثره أصعب ملاحظة فوراً — يستاهل وقفة بشرية دائماً بصرف النظر عن حجم المادة.

### توجيه للفريق
- عضو 5 وعضو 4 يبدآن فوراً بالتوازي (لا اعتماديات). عضو 3 يبدأ الواجهة بعقد mock للدالتين الجديدتين لحين جهوزية عضو 5.

---

## [التاريخ: 2026-07-20 — جلسة 3]

### ما تم إنجازه
- 🔴 اكتشاف حرج بطلب مباشر من المستخدم: مادة جديدة تُنشر من لوحة التحكم لا تظهر بصفحة "المواد". فحصت `githubPublisher.js` الفعلي (طلبته من المستخدم لأن نسختي المحلية قديمة) ووجدت الإصلاح المُوصَى به بتقرير عضو 6 **منفَّذ فعلاً** بذاك الملف — لكن `AdminSubjectEditor.jsx`/`SubjectForm.jsx` لا يجلبان ولا يمرّران `existingStudyPlan` إطلاقاً لدالة البناء.
- الأثر الفعلي أخطر من "مادة لا تظهر": بما إن `existingStudyPlan` يصل دائماً فارغاً، كل عملية نشر كانت **تكتب `study-plan.json` من الصفر بمادة واحدة فقط** — تأكدت من المستخدم إنه نشر فعلاً مادة جديدة من قبل، يعني احتمال حقيقي لفقدان "الدارات"/"قواعد المعطيات" من القائمة (الملفات الفعلية بمجلداتها سليمة، فقط القائمة المجمّعة تأثرت).
- أصلحت 3 ملفات مباشرة (`AdminSubjectEditor.jsx`, `SubjectForm.jsx`, `PublishPanel.jsx`) — جلب/تمرير `existingStudyPlan` كاملاً عبر السلسلة.
- أضفت بنفس الجلسة ميزتين إضافيتين طلبهما المستخدم: (أ) اختيار مادة من الخطة الدراسية الرسمية (`curriculum.js` + `CurriculumCoursePicker.jsx` جديدان) عند إضافة مادة جديدة بدل الكتابة اليدوية، (ب) تصفير قائمة الملفات المرفوعة تلقائياً بعد نشر ناجح (منع إعادة رفع نفس الملفات بالغلط) عبر `onPublishSuccess` + إعادة تركيب `FileUploaderWidget` بـ `key`.
- سلّمت الحزم بمسارات مجلدات صحيحة مطابقة لبنية الريبو (بعد ملاحظة من المستخدم إن التسليم الأول كان مسطّحاً بلا مسارات).

### مشكلات واجهتها
- نسخي المحلية من `AdminSubjectEditor.jsx`/`FileUploaderWidget.jsx`/`PublishPanel.jsx`/`SubjectForm.jsx`/`githubPublisher.js` كانت متأخرة كثيراً عن الكود الحي فعلياً (تطوّر مباشر على الإنتاج تجاوز آخر تسليم وصلني). طلبت الملفات الفعلية من المستخدم قبل أي تعديل بدل البناء على افتراضات قديمة.
- لا أقدر أتصفح ملفات GitHub الفرعية آلياً (`tree`/`blob` محظورة بـ robots.txt من طرف GitHub نفسه) — فقط صفحة الريبو الرئيسية تُقرأ.

### قرارات اتخذتها (ولماذا)
- بيانات `curriculum.json` (الخطة الرسمية لأربع سنوات) بقيت **ملفاً منفصلاً تماماً** عن `study-plan.json` (القائمة المسطّحة للمواد ذات المحتوى الفعلي) — دمجهما كان سيكسر عقد `useStudyPlan()` المستخدم فعلياً بالمفضلة/آخر الزيارات/القائمة.
- من الآن فصاعداً: أعطي "ملفات متغيّرة فقط" بمسارات صحيحة كاملة، لا حزمة مشروع كاملة — لتفادي استبدال بيانات حية (`public/data/subjects/**`, `study-plan.json`) بنسخي القديمة عند أي رفع.

### توجيه للفريق
- طلبت من المستخدم إرسال محتوى `study-plan.json` الحي الحالي للتحقق من مدى الضرر الفعلي وإصلاحه إن لزم.

---

## [التاريخ: 2026-07-20 — جلسة 2]

### ما تم إنجازه
- استخرجت الخطة الدراسية الكاملة (4 سنوات، 8 مستويات، تخصصات بالمستويين 7/8) من المشروع الأصلي (`4cml/assistant404` المرفوع كملف)، ونسختها لملف `curriculum.json` جديد بمشروع v2، وأعدت بناء `StudyPlan.jsx` بنفس أسلوب الأكورديون الأصلي مكيَّفاً لتوكنز الثيم ومسارات BASE_URL الآمنة.
- بنيت ميزة "قائمة جانبية منسدلة" (Sidebar drawer) للجوال — زر هامبرجر بـ `Header.jsx`، انزلاق حقيقي بـ `Sidebar.jsx`، حالة مشتركة بـ `App.jsx` — بالكامل ملفات المدير، بلا حاجة لأي عضو.
- حللت وأصلحت سلسلة أعطال نشر إنتاج حقيقية بالتتابع: اسم مستودع افتراضي خاطئ بلوحة النشر، `basename` مفقود من `BrowserRouter`، غياب `404.html` لدعم توجيه React Router على GitHub Pages، 12 موضع `fetch`/رابط ملف بمسار مطلق (يتجاهل `base path`)، صلاحية `Pull requests` ناقصة بتوكن GitHub، اسم امتداد `.pdf` مفقود بتنزيل الملفات.
- وثّقت قاعدة صريحة بعد حادثة إعادة تسمية الريبو مرتين (`assistant_404` ← `QPU_assistant_404`): أي `base`/اسم مستودع افتراضي بالكود لازم يطابق اسم الريبو الفعلي حرفياً، وأتحقق منه كل مرة يتغيّر فيها اسم الريبو.

### مشكلات واجهتها
- تشخيصات متكررة بسبب معلومات ناقصة أول مرة (اسم الريبو الفعلي، حالة تفعيل Pages، محتوى `vite.config.js` الفعلي بالريبو) — كل مرة طلبت تأكيداً مباشراً من المستخدم بدل الافتراض، بدل تكرار حل خاطئ.
- لا صلاحية وصول لبيئة متصفح فعلية عندي لاختبار بصري حي — الاعتماد الكامل على وصف المستخدم لما يراه + فحص الكود البرمجي فقط.

### قرارات اتخذتها (ولماذا)
- توحيد توكن دخول لوحة التحكم مع توكن النشر (نفس GitHub PAT، `sessionStorage` فقط) — يبسّط التجربة، يزيل الحاجة لكلمة سر منفصلة.
- دمج تلقائي افتراضي (`autoMerge: true`) للنشر رغم المخاطرة الأمنية النظرية (توكن بالذاكرة + دمج بلا مراجعة بشرية) — قرار واعٍ بالمخاطرة نظراً لصغر الفريق وغياب محتوى مستخدمين غير موثوق بالموقع؛ خيار "لا تدمج تلقائياً" متاح كسقف أمان لمن يحتاجه.

### توجيه للفريق
- لا عمل جديد مطلوب من أي عضو بهذي الجلسة — كل الإصلاحات كانت ضمن ملفات المدير أو دعم إنتاج مباشر بطلب صريح من المستخدم.

---

## [التاريخ: 2026-07-19 — جلسة 1 (تأسيس المشروع)]

### ما تم إنجازه
- صممت خطة بناء كاملة من الصفر (`team-plan-v2.md`) بمبدأ معماري واحد يحل فشل المشروع الأول: **عقد أولاً + مالك واحد لكل ملف بلا استثناء**، مع غلاف عام (`App.jsx`, `Sidebar.jsx`, `Header.jsx`) أبنيه أنا مسبقاً بمساحات فارغة محجوزة، فلا يحتاج أي عضو يلمسه أثناء العمل المتوازي.
- حسمت كل العقود مسبقاً: توكنز الثيم، شكل `subject.json`/`professorVariants`، حقل `hidden` على 3 مستويات، `SECTION_LABELS`، `id` كـ slug دائماً.
- جهّزت هيكل "اليوم 0" فعلياً (كود يعمل، لا وصف فقط) ووزّعته على 6 أعضاء.
- راجعت 3 جولات تسليم متتالية: رصدت تسليماً يتيماً غير مكتشَف من عضو 6 (جولة 1)، أكّدت خللاً حرجاً بمنطق `setActive` بتتبّع يدوي سطراً بسطر (جولة 2)، اعتمدت الإصلاحات نهائياً بعد تحقق مستقل كامل (جولة 3).
- حسمت تعارض تسمية `isVisible`/`filterVisible` لصالح الاسم الفعلي المُستخدَم بالكود (لا الوثيقة)، واعتمدت استثناء `jszip` بـ `package.json` كأول استثناء موثَّق لقاعدة الملكية الحصرية.

### مشكلات واجهتها
- بيئتي بلا اتصال شبكة — لم أقدر أشغّل `npm install/build` بنفسي أغلب الجولات، اعتمدت على تحقق عضو 6 المستقل + فحصي البرمجي الخاص (مسارات استيراد، خرق ملكية) كتعويض.

### قرارات اتخذتها (ولماذا)
- تجميد ملفات محورية (`App.jsx`, `Sidebar.jsx`, `Header.jsx`, `sectionLabels.js`) بيدي فقط — يمنع بالضبط نوع التعارض المزدوج الذي أفشل المشروع الأول على `Subject.jsx` مرتين.

### توجيه للفريق
- بعد الجولة 3: لا مزيد من إصلاحات مطلوبة، المشروع جاهز فنياً للدمج بـ `main` بانتظار اختبار متصفح/جوال بشري فعلي (قيد لا يقدر عليه أي طرف آلي بهذي البيئة).

---
